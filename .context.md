# typescript@5.9.3 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-14
> Drop this file into your project and reference it with @context.md in Cursor/Claude

TypeScript is JavaScript with syntax for types.

---

## typescript

**Install:**
```bash
npm install typescript --save-dev
```

**Import:**
```typescript
typescript
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- TypeScript requires Node.js to run
- TypeScript has its own configuration file (tsconfig.json)
- TypeScript has different module systems (CommonJS, ES6, etc.)
- TypeScript has its own type checking system

**Usage:**
```typescript
npx tsc --init
```
