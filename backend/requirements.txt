fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic>=2.11.0
python-dotenv==1.0.1
# Playwright for JS-rendered docs sites
playwright>=1.50.0
# HTML to Markdown conversion
html2text==2024.2.26
# Redis (production cache)
redis==5.1.0
# Supabase (production storage)
supabase==2.9.0
# PDF/document conversion via IBM Docling (optional — install separately)
# docling==2.0.0
# Google Gemini
google-generativeai==0.8.3
# Auth
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
cryptography==46.0.5