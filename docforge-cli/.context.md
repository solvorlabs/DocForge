

---

# bits-ui@0.21.5 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A headless UI component library for Svelte

---

## Accordion

**Install:**
```bash
npm install bits-ui@0.21.5
```

**Import:**
```typescript
import { Accordion } from 'bits-ui';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| type | string | — | 'single' |
| value | string | string[] | — | null |
| disabled | boolean | — | false |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required directives for Svelte
- Deprecated props or methods from previous versions
- Breaking changes between major versions

**Usage:**
```tsx
<Accordion.Root type='single'><Accordion.Item value='item-1'><Accordion.Header><Accordion.Trigger>Item 1 Title</Accordion.Trigger></Accordion.Header><Accordion.Content>This is the collapsible content for this section.</Accordion.Content></Accordion.Item></Accordion.Root>
```

---

## AlertDialog

**Install:**
```bash
npm install bits-ui@0.21.5
```

**Import:**
```typescript
import { AlertDialog } from 'bits-ui';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| open | boolean | — | false |
| onOpenAutoFocus | (e: FocusEvent) => void | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required directives for Svelte
- Deprecated props or methods from previous versions
- Breaking changes between major versions

**Usage:**
```tsx
<AlertDialog.Root><AlertDialog.Trigger>Open Dialog</AlertDialog.Trigger><AlertDialog.Portal><AlertDialog.Overlay /><AlertDialog.Content>This is the dialog content.</AlertDialog.Content></AlertDialog.Portal></AlertDialog.Root>
```

---

## AspectRatio

**Install:**
```bash
npm install bits-ui@0.21.5
```

**Import:**
```typescript
import { AspectRatio } from 'bits-ui';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| ratio | number | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required directives for Svelte
- Deprecated props or methods from previous versions
- Breaking changes between major versions

**Usage:**
```tsx
<AspectRatio.Root ratio={16 / 9}><!-- content --></AspectRatio.Root>
```

---

## Avatar

**Install:**
```bash
npm install bits-ui@0.21.5
```

**Import:**
```typescript
import { Avatar } from 'bits-ui';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| src | string | ✓ | null |
| alt | string | ✓ | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required directives for Svelte
- Deprecated props or methods from previous versions
- Breaking changes between major versions

**Usage:**
```tsx
<Avatar.Root><Avatar.Image src='https://github.com/huntabyte.png' alt='Huntabyte's avatar' /><Avatar.Fallback>HB</Avatar.Fallback></Avatar.Root>
```

---

# shadcn-svelte@0.9.4 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

An unofficial community-led Svelte port of shadcn/ui

---

## Accordion

**Install:**
```bash
shadcn-svelte@latest add accordion
```

**Import:**
```typescript
$lib/components/ui/accordion/index.js
```

**Peer Dependencies:**
- `bits-ui`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| type | string | — | single |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires bits-ui dependency

**Usage:**
```tsx
<Accordion.Root type="single"><Accordion.Item value="item-1"><Accordion.Trigger>Is it accessible?</Accordion.Trigger><Accordion.Content>Yes. It adheres to the WAI-ARIA design pattern.</Accordion.Content></Accordion.Item></Accordion.Root>
```

---

## Alert Dialog

**Install:**
```bash
shadcn-svelte@latest add alert-dialog
```

**Import:**
```typescript
$lib/components/ui/alert-dialog/index.js
```

**Peer Dependencies:**
- `bits-ui`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| onClose | function | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires bits-ui dependency

**Usage:**
```tsx
<AlertDialog.Root><AlertDialog.Trigger>Open</AlertDialog.Trigger><AlertDialog.Content><AlertDialog.Header><AlertDialog.Title>Are you absolutely sure?</AlertDialog.Title><AlertDialog.Description>This action cannot be undone. This will permanently delete your account and remove your data from our servers.</AlertDialog.Description></AlertDialog.Header><AlertDialog.Footer><AlertDialog.Cancel>Cancel</AlertDialog.Cancel><AlertDialog.Action>Continue</AlertDialog.Action></AlertDialog.Footer></AlertDialog.Content></AlertDialog.Root>
```

---

## Alert

**Install:**
```bash
shadcn-svelte@latest add alert
```

**Import:**
```typescript
$lib/components/ui/alert/index.js
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| variant | string | — | default |

**Usage:**
```tsx
<Alert.Root><Alert.Title>Heads up!</Alert.Title><Alert.Description>You can add components to your app using the cli.</Alert.Description></Alert.Root>
```

---

## Aspect Ratio

**Install:**
```bash
shadcn-svelte@latest add aspect-ratio
```

**Import:**
```typescript
$lib/components/ui/aspect-ratio/index.js
```

**Peer Dependencies:**
- `bits-ui`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| ratio | number | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires bits-ui dependency

**Usage:**
```tsx
<div class="w-[450px]"><AspectRatio ratio={16 / 9} class="bg-muted"><img src="..." alt="..." class="rounded-md object-cover" /></AspectRatio></div>
```

---

## Avatar

**Install:**
```bash
shadcn-svelte@latest add avatar
```

**Import:**
```typescript
$lib/components/ui/avatar/index.js
```

**Peer Dependencies:**
- `bits-ui`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| src | string | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires bits-ui dependency

**Usage:**
```tsx
<Avatar.Root><Avatar.Image src="https://github.com/shadcn.png" alt="@shadcn" /><Avatar.Fallback>CN</Avatar.Fallback></Avatar.Root>
```

---

## Badge

**Install:**
```bash
shadcn-svelte@latest add badge
```

**Import:**
```typescript
$lib/components/ui/badge/index.js
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| variant | string | — | default |

**Usage:**
```tsx
<Badge variant="outline">Badge</Badge>
```

---

## Breadcrumb

**Install:**
```bash
shadcn-svelte@latest add breadcrumb
```

**Import:**
```typescript
$lib/components/ui/breadcrumb/index.js
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| separator | string | — | / |

**Usage:**
```tsx
<Breadcrumb.Root><Breadcrumb.List><Breadcrumb.Item><Breadcrumb.Link href="/">Home</Breadcrumb.Link></Breadcrumb.Item><Breadcrumb.Separator /><Breadcrumb.Item><Breadcrumb.Link href="/components">Components</Breadcrumb.Link></Breadcrumb.Item><Breadcrumb.Separator /><Breadcrumb.Item><Breadcrumb.Page>Breadcrumb</Breadcrumb.Page></Breadcrumb.Item></Breadcrumb.List></Breadcrumb.Root>
```

---

## Button Group

**Install:**
```bash
shadcn-svelte@latest add button-group
```

**Import:**
```typescript
$lib/components/ui/button-group/index.js
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| orientation | string | — | horizontal |

**Usage:**
```tsx
<ButtonGroup.Root><Button>Button 1</Button><Button>Button 2</Button></ButtonGroup.Root>
```

---

## Button

**Install:**
```bash
shadcn-svelte@latest add button
```

**Import:**
```typescript
$lib/components/ui/button/index.js
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| variant | string | — | default |

**Usage:**
```tsx
<Button variant="outline">Button</Button>
```

---

# skeleton@3.1.2 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

An adaptive design system powered by Tailwind CSS

---

## skeleton

**Install:**
```bash
pnpm install @skeletonlabs/skeleton
```

**Import:**
```typescript
import { Skeleton } from '@skeletonlabs/skeleton';
```

**Peer Dependencies:**
- `tailwindcss`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS
- Must be used with a compatible web framework

**Usage:**
```tsx
import { Skeleton } from '@skeletonlabs/skeleton';
const App = () => {
  return <Skeleton />;
};
```

---

## skeleton-react

**Install:**
```bash
pnpm install @skeletonlabs/skeleton-react
```

**Import:**
```typescript
import { Skeleton } from '@skeletonlabs/skeleton-react';
```

**Peer Dependencies:**
- `react`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React
- Must be used with a compatible React version

**Usage:**
```tsx
import { Skeleton } from '@skeletonlabs/skeleton-react';
const App = () => {
  return <Skeleton />;
};
```

---

## skeleton-svelte

**Install:**
```bash
pnpm install @skeletonlabs/skeleton-svelte
```

**Import:**
```typescript
import { Skeleton } from '@skeletonlabs/skeleton-svelte';
```

**Peer Dependencies:**
- `svelte`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Svelte
- Must be used with a compatible Svelte version

**Usage:**
```tsx
import { Skeleton } from '@skeletonlabs/skeleton-svelte';
const App = () => {
  return <Skeleton />;
};
```

---

# daisyui@5.0.2 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A Tailwind CSS plugin for building UI components

---

## Accordion

**Install:**
```bash
npm i -D daisyui@latest
```

**Import:**
```typescript
import { Accordion } from 'daisyui';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| active | boolean | — | — |
| collapsible | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed
- Needs to be used with the 'daisyui' plugin in tailwind.config.js

**Usage:**
```tsx
<Accordion active={true} collapsible={true} />
```

---

## Alert

**Install:**
```bash
npm i -D daisyui@latest
```

**Import:**
```typescript
import { Alert } from 'daisyui';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| type | string | — | — |
| message | string | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed
- Needs to be used with the 'daisyui' plugin in tailwind.config.js

**Usage:**
```tsx
<Alert type='success' message='This is a success message' />
```

---

## Avatar

**Install:**
```bash
npm i -D daisyui@latest
```

**Import:**
```typescript
import { Avatar } from 'daisyui';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| src | string | ✓ | — |
| alt | string | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed
- Needs to be used with the 'daisyui' plugin in tailwind.config.js

**Usage:**
```tsx
<Avatar src='https://example.com/image.jpg' alt='Example Image' />
```

---

# flowbite-svelte@0.47.1 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Flowbite Svelte is an official Flowbite component library for Svelte

---

## Accordion

**Install:**
```bash
pnpm i -D flowbite-svelte flowbite
```

**Import:**
```typescript
import { Accordion, AccordionItem } from 'flowbite-svelte';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| activeClass | string | — | — |
| inactiveClass | string | — | — |
| flush | boolean | — | — |
| multiple | boolean | — | — |
| open | boolean | — | — |
| transitionParams | object | — | — |
| transitionType | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed and configured
- Needs to import 'flowbite-svelte' and 'flowbite' packages
- Accordion items must be used inside an Accordion component

**Usage:**
```tsx
<Accordion><AccordionItem>Content</AccordionItem></Accordion>
```

---

## Alert

**Install:**
```bash
pnpm i -D flowbite-svelte flowbite
```

**Import:**
```typescript
import { Alert } from 'flowbite-svelte';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| border | boolean | — | — |
| color | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed and configured
- Needs to import 'flowbite-svelte' and 'flowbite' packages

**Usage:**
```tsx
<Alert>Content</Alert>
```

---

# @ark-ui/svelte@4.2.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Build scalable design systems with unstyled, accessible UI components

---

## Dialog

**Install:**
```bash
npm install @ark-ui/svelte
```

**Import:**
```typescript
import { Dialog } from '@ark-ui/svelte/dialog'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| open | boolean | — | false |
| onOpenChange | (open: boolean) => void | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual CSS imports for styling
- Uses Zag.js state machines for predictable behavior

**Usage:**
```tsx
<Dialog.Root><Dialog.Trigger>Open Dialog</Dialog.Trigger><Dialog.Content>Dialog Content</Dialog.Content></Dialog.Root>
```

---

# svelte-ux@0.78.3 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A library to simplify building highly interactive and visual applications

---

## svelte-ux

**Install:**
```bash
pnpm i svelte-ux
```

**Import:**
```typescript
import { Component } from 'svelte-ux';
```

**Peer Dependencies:**
- `@layerstack/svelte-actions`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| name | string | — | null |
| kind | string | — | null |
| constant | boolean | — | null |
| type | string | — | null |
| value | any | — | null |
| description | string | — | null |
| isFunction | boolean | — | null |
| isFunctionDeclaration | boolean | — | null |
| isRequired | boolean | — | null |
| reactive | boolean | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required peer dependencies must be installed separately
- CSS imports must be added manually
- Configuration file changes may be required

**Usage:**
```tsx
import { Component } from 'svelte-ux';

const MyComponent = () => {
  return <Component name='my-component' kind='let' constant={true} type='string' value='Hello World' description='My component' isFunction={false} isFunctionDeclaration={false} isRequired={false} reactive={true} />;
};
```

---

# carbon-components-svelte@0.85.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Svelte implementation of the Carbon Design System

---

## Accordion

**Install:**
```bash
npm i carbon-components-svelte
```

**Import:**
```typescript
carbon-components-svelte
```

**Peer Dependencies:**
- `carbon-components`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| title | string | ✓ | — |
| open | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required CSS imports
- Deprecated props or methods from previous versions

**Usage:**
```tsx
<Accordion><AccordionItem title="Section 1" open> Content 1 </AccordionItem></Accordion>
```

---

# svelte-material-ui@8.0.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Svelte Material UI library

---

## svelte-material-ui

**Install:**
```bash
npm install svelte-material-ui@8.0.0
```

**Import:**
```typescript
import { Component } from 'svelte-material-ui';
```

**Peer Dependencies:**
- `@material-ui/core`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| class | string | — | — |
| style | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required peer dependency: @material-ui/core
- Breaking change from v7: Removed deprecated props
- Common misuse pattern: Wrong import path

**Usage:**
```tsx
import { Button } from 'svelte-material-ui';
<Button>Click me</Button>
```

---

# @svelteuidev/core@0.15.7 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Svelte UI core library

---

## @svelteuidev/core

**Install:**
```bash
npm install @svelteuidev/core or yarn add @svelteuidev/core
```

**Import:**
```typescript
import { Button, Checkbox } from '@svelteuidev/core'
```

**Peer Dependencies:**
- `@svelteuidev/prism`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | string | — | 'gray' |
| variant | string | — | 'filled' |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required peer dependency: @svelteuidev/prism
- Breaking change in v0.15.0: renamed 'theme' prop to 'colorScheme'
- Common misuse pattern: wrong import path, use '@svelteuidev/core' instead of '@svelteui/core'

**Usage:**
```tsx
import { Button } from '@svelteuidev/core'; <Button color='blue'>Click me</Button>
```

---

# layerchart@0.52.1 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A large collection of composable Svelte chart components to build a wide range of visualizations

---

## LayerChart

**Install:**
```bash
pnpm i layerchart@0.52.1
```

**Import:**
```typescript
import { LayerChart } from 'layerchart';
```

**Peer Dependencies:**
- `svelte`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| data | array | ✓ | — |
| width | number | — | 500 |
| height | number | — | 300 |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required peer dependencies must be installed separately
- CSS imports must be added manually

**Usage:**
```tsx
import { LayerChart } from 'layerchart';
<LayerChart data={data} width={600} height={400} />
```

---

# sveltestrap@6.2.1 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Documentation for sveltestrap@6.2.1

---

## sveltestrap

**Install:**
```bash
npm install sveltestrap
```

**Import:**
```typescript
import { sveltestrap } from 'sveltestrap'
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- ⚠️  Gemini API key not configured — run with GEMINI_API_KEY set for real extraction

**Usage:**
```tsx
<sveltestrap />
```

---

# @threlte/core@8.1.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Documentation for @threlte/core@8.1.0

---

## @threlte/core

**Install:**
```bash
npm install @threlte/core
```

**Import:**
```typescript
import { @threlte/core } from '@threlte/core'
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- ⚠️  Gemini API key not configured — run with GEMINI_API_KEY set for real extraction

**Usage:**
```tsx
<@threlte/core />
```

---

# sveltekit-superforms@2.11.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A SvelteKit form library that brings a comprehensive solution for server and client form validation

---

## Superform

**Install:**
```bash
npm i -D sveltekit-superforms
```

**Import:**
```typescript
import { superForm } from 'sveltekit-superforms';
```

**Peer Dependencies:**
- `sveltekit`
- `arktype`
- `superstruct`
- `typebox`
- `valibot`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| form | object | ✓ | — |
| errors | object | — | — |
| constraints | object | — | — |
| message | string | — | — |
| enhance | function | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Define the schema outside the load function to make caching work
- Always return the form object to the client in load functions and form actions
- Use the correct import path for the adapter and validation library

**Usage:**
```typescript
import { superForm } from 'sveltekit-superforms';
const { form, errors, constraints, message } = superForm(data.form);
```

---

## SuperDebug

**Install:**
```bash
npm i -D sveltekit-superforms
```

**Import:**
```typescript
import SuperDebug from 'sveltekit-superforms';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| data | object | ✓ | — |

**Usage:**
```tsx
import SuperDebug from 'sveltekit-superforms';
<SuperDebug data={$form} />
```

---

# attractions@0.11.2 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-16
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A pretty cool UI kit for Svelte

---

## attractions

**Install:**
```bash
npm install --save-dev attractions
```

**Import:**
```typescript
import { Button } from 'attractions';
```

**Peer Dependencies:**
- `svelte-preprocess`
- `sass`
- `postcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| filled | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- This UI kit is now deprecated
- Requires svelte-preprocess, Dart Sass and PostCSS to be installed separately
- Requires svelte-preprocess to be added to the preprocessor chain

**Usage:**
```tsx
<Button>click me</Button>
```
 