{'library': 'pkg.json', 'version': '2.0.1', 'description': 'Get the package.json content either from npm or from a git repository.', 'components': [{'name': 'pkg.json', 'import_path': 'const packageJson = require("pkg.json");', 'install_command': 'npm install --save pkg.json', 'props': [{'name': 'input', 'type': 'String', 'required': True, 'default': None}, {'name': 'version', 'type': 'String', 'required': False, 'default': None}, {'name': 'callback', 'type': 'Function', 'required': True, 'default': None}], 'gotchas': ['The input parameter can be either an npm package name or a git url.', 'The version parameter can be an npm package version or a git sha/tag/branch/etc.'], 'peer_dependencies': [], 'usage_example': 'packageJson("git-stats", function (err, data) { console.log(err || data); });', 'last_verified': '2026-03-17'}], 'cached_at': '2026-03-17'}

---

# @ark-ui/svelte@5.19.1 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A headless UI component library for Svelte

---

## Swap

**Install:**
```bash
npm install @ark-ui/svelte
```

**Import:**
```typescript
import { Swap } from '@ark-ui/svelte/swap'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| swap | boolean | — | false |
| lazyMount | boolean | — | false |
| unmountOnExit | boolean | — | false |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual CSS imports for styling
- Uses state machines for predictable behavior

**Usage:**
```tsx
import { Swap } from '@ark-ui/svelte/swap';
<Swap.Root swap={true}>
  <Swap.Indicator />
</Swap.Root>
```

---

## Accordion

**Install:**
```bash
npm install @ark-ui/svelte
```

**Import:**
```typescript
import { Accordion } from '@ark-ui/svelte/accordion'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| collapsible | boolean | — | false |
| defaultValue | string[] | — | null |
| disabled | boolean | — | false |
| id | string | — | null |
| ids | object | — | null |
| lazyMount | boolean | — | false |
| multiple | boolean | — | false |
| onFocusChange | (details: FocusChangeDetails) => void | — | null |
| onValueChange | (details: ValueChangeDetails) => void | — | null |
| orientation | 'horizontal' | 'vertical' | — | 'vertical' |
| unmountOnExit | boolean | — | false |
| value | string[] | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual CSS imports for styling
- Uses state machines for predictable behavior

**Usage:**
```tsx
import { Accordion } from '@ark-ui/svelte/accordion';
<Accordion.Root>
  <Accordion.Item>
    <Accordion.ItemTrigger>
      <Accordion.ItemIndicator />
    </Accordion.ItemTrigger>
    <Accordion.ItemContent />
  </Accordion.Item>
</Accordion.Root>
```

---

# @melt-ui/svelte@0.86.6 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Melt UI Svelte library

---

## @melt-ui/svelte

**Install:**
```bash
npm install @melt-ui/svelte@0.86.6
```

**Import:**
```typescript
import { Component } from '@melt-ui/svelte'
```

**Peer Dependencies:**
- `svelte@3.44`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Svelte 3.44 or higher
- CSS imports must be added manually

**Usage:**
```tsx
import { Button } from '@melt-ui/svelte';
<Button on:click={() => console.log('clicked')}>Click me</Button>
```

---

# bits-ui@2.16.3 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

bits-ui library documentation

---

## bits-ui

**Install:**
```bash
npm install bits-ui@2.16.3
```

**Import:**
```typescript
import { Button, Input } from 'bits-ui'
```

**Peer Dependencies:**
- `react >= 17.0.2`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| disabled | boolean | — | false |
| onClick | function | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required peer dependency: react >= 17.0.2
- Breaking change in v2: removed 'type' prop from Button component

**Usage:**
```tsx
import React from 'react'; import { Button } from 'bits-ui'; const App = () => { return <Button onClick={() => console.log('clicked')}>Click me</Button>; };
```

---

# daisyui@5.5.19 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A Tailwind CSS plugin for building UI components

---

## Accordion

**Install:**
```bash
npm i -D daisyui@latest
```

**Import:**
```typescript
import { Accordion } from 'daisyui';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| active | boolean | — | — |
| collapsible | boolean | — | True |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed
- Must be used with the 'daisyui' plugin

**Usage:**
```tsx
<Accordion active={true} collapsible={false} />
```

---

## Alert

**Install:**
```bash
npm i -D daisyui@latest
```

**Import:**
```typescript
import { Alert } from 'daisyui';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| type | string | — | info |
| message | string | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed
- Must be used with the 'daisyui' plugin

**Usage:**
```tsx
<Alert type='success' message='This is a success message' />
```

---

## Avatar

**Install:**
```bash
npm i -D daisyui@latest
```

**Import:**
```typescript
import { Avatar } from 'daisyui';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| src | string | ✓ | — |
| alt | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed
- Must be used with the 'daisyui' plugin

**Usage:**
```tsx
<Avatar src='https://example.com/image.jpg' alt='User Avatar' />
```

---

# flowbite-svelte@1.31.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Flowbite Svelte is an official Flowbite component library for Svelte

---

## Accordion

**Install:**
```bash
pnpm i -D flowbite-svelte flowbite
```

**Import:**
```typescript
import { Accordion, AccordionItem } from 'flowbite-svelte';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| multiple | boolean | — | false |
| open | boolean | — | false |
| activeClass | string | — | null |
| inactiveClass | string | — | null |
| flush | boolean | — | false |
| transitionType | string | — | 'slide' |
| transitionParams | object | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed and configured
- Needs to import the flowbite-svelte-icons package for icons

**Usage:**
```tsx
<Accordion><AccordionItem>Content</AccordionItem></Accordion>
```

---

## Alert

**Install:**
```bash
pnpm i -D flowbite-svelte flowbite
```

**Import:**
```typescript
import { Alert } from 'flowbite-svelte';
```

**Peer Dependencies:**
- `tailwindcss`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | string | — | null |
| border | boolean | — | false |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Tailwind CSS to be installed and configured

**Usage:**
```tsx
<Alert>Content</Alert>
```

---

# layerchart@1.0.13 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A library for creating layer charts

---

## LayerChart

**Install:**
```bash
npm install layerchart@1.0.13
```

**Import:**
```typescript
import LayerChart from 'layerchart'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| data | Array<Object> | ✓ | — |
| width | number | — | 500 |
| height | number | — | 300 |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual CSS import: import 'layerchart/dist/layerchart.css';
- Does not support IE11

**Usage:**
```tsx
import LayerChart from 'layerchart'; const chart = <LayerChart data={data} width={800} height={600} />;
```

---

# shadcn-svelte@1.2.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A Svelte library

---

## shadcn-svelte

**Install:**
```bash
npm install shadcn-svelte@1.2.0
```

**Import:**
```typescript
import { Component } from 'shadcn-svelte'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| class | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual CSS import: import 'shadcn-svelte/dist/index.css';

**Usage:**
```tsx
<script>import { Component } from 'shadcn-svelte';</script><Component class="custom-class" />
```

---

# skeleton@4.13.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A library for building web applications

---

## skeleton

**Install:**
```bash
npm install skeleton@4.13.0
```

**Import:**
```typescript
import { Skeleton } from 'skeleton'
```

**Peer Dependencies:**
- `react`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| loading | boolean | — | true |
| width | number | string | — | 100% |
| height | number | string | — | 100% |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required peer dependency: react >= 17.0.0
- Deprecated prop: 'animation' (use 'animate' instead)
- Breaking change: 'skeleton' component now uses CSS grid by default

**Usage:**
```tsx
import { Skeleton } from 'skeleton'; <Skeleton loading={true} width={200} height={100} />
```

---

# svelte-kit@1.2.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Documentation for svelte-kit@1.2.0

---

## svelte-kit

**Install:**
```bash
npm install svelte-kit
```

**Import:**
```typescript
import { svelte-kit } from 'svelte-kit'
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- ⚠️  Gemini API key not configured — run with GEMINI_API_KEY set for real extraction

**Usage:**
```tsx
<svelte-kit />
```

---

# svelte-motion@0.12.2 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A Svelte library for animations and gestures

---

## svelte-motion

**Install:**
```bash
npm install svelte-motion@0.12.2
```

**Import:**
```typescript
import { motion } from 'svelte-motion'
```

**Peer Dependencies:**
- `svelte`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| initial | object | — | null |
| animate | object | — | null |
| transition | object | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Svelte 3.31 or higher
- Does not work with server-side rendering (SSR) out of the box

**Usage:**
```tsx
import { motion } from 'svelte-motion';

<motion initial={{ x: 0 }} animate={{ x: 100 }}>
  <div>Animated div</div>
</motion>
```

---

# @sveltejs/adapter-auto@7.0.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Automatically installs and uses the correct adapter for supported environments when you deploy

---

## @sveltejs/adapter-auto

**Install:**
```bash
npx sv create
```

**Import:**
```typescript
import adapter from '@sveltejs/adapter-auto';
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- Must install the appropriate adapter to devDependencies once a target environment is settled on
- Does not take any options, must install the underlying adapter for configuration options

**Usage:**
```typescript
import adapter from '@sveltejs/adapter-auto';
const config = {
  kit: {
    adapter: adapter()
  }
};
```

---

## @sveltejs/adapter-cloudflare

**Install:**
```bash
npm i -D @sveltejs/adapter-cloudflare
```

**Import:**
```typescript
import adapter from '@sveltejs/adapter-cloudflare';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| config | string | — | undefined |
| platformProxy | object | — | undefined |
| fallback | string | — | 'plaintext' |
| routes | object | — | undefined |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Must have a Wrangler configuration file in the project root for Cloudflare Workers

**Usage:**
```typescript
import adapter from '@sveltejs/adapter-cloudflare';
const config = {
  kit: {
    adapter: adapter({
      // options
    })
  }
};
```

---

## @sveltejs/adapter-netlify

**Install:**
```bash
npm i -D @sveltejs/adapter-netlify
```

**Import:**
```typescript
import adapter from '@sveltejs/adapter-netlify';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| edge | boolean | — | false |
| split | boolean | — | false |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Must have a netlify.toml file in the project root

**Usage:**
```typescript
import adapter from '@sveltejs/adapter-netlify';
const config = {
  kit: {
    adapter: adapter({
      // options
    })
  }
};
```

---

## @sveltejs/adapter-vercel

**Install:**
```bash
npm i -D @sveltejs/adapter-vercel
```

**Import:**
```typescript
import adapter from '@sveltejs/adapter-vercel';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| runtime | string | — | undefined |
| regions | array | — | undefined |
| split | boolean | — | false |
| external | array | — | undefined |
| memory | number | — | 1024 |
| maxDuration | number | — | 10 |
| isr | object | — | undefined |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Must have a Vercel project set up

**Usage:**
```typescript
import adapter from '@sveltejs/adapter-vercel';
const config = {
  kit: {
    adapter: adapter({
      // options
    })
  }
};
```

---

## @sveltejs/adapter-node

**Install:**
```bash
npm i -D @sveltejs/adapter-node
```

**Import:**
```typescript
import adapter from '@sveltejs/adapter-node';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| out | string | — | 'build' |
| precompress | boolean | — | true |
| envPrefix | string | — | '' |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Must have a Node.js environment set up

**Usage:**
```typescript
import adapter from '@sveltejs/adapter-node';
const config = {
  kit: {
    adapter: adapter({
      // options
    })
  }
};
```

---

# @sveltejs/kit@2.50.2 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A framework for building web applications using Svelte

---

## SvelteKit

**Install:**
```bash
npx sv create
```

**Import:**
```typescript
import { kit } from '@sveltejs/kit'
```

**Peer Dependencies:**
- `svelte`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| target | string | ✓ | — |
| props | object | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required directives ('use client', 'use server')
- Deprecated props or methods from previous versions
- Breaking changes between major versions (e.g. v4 → v5 API changes)
- Required peer dependencies that must be installed separately
- Common misuse patterns (wrong import path, wrong prop name)

**Usage:**
```typescript
import { kit } from '@sveltejs/kit'; const app = kit({ target: document.getElementById('app') });
```

---

# @sveltejs/vite-plugin-svelte@6.2.4 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Vite plugin for Svelte

---

## @sveltejs/vite-plugin-svelte

**Install:**
```bash
npm install --save-dev @sveltejs/vite-plugin-svelte@6.2.4
```

**Import:**
```typescript
import { svelte } from '@sveltejs/vite-plugin-svelte'
```

**Peer Dependencies:**
- `vite`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| compilerOptions | object | — | null |
| emitCss | boolean | — | true |
| hot | object | boolean | — | true |
| kit | object | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Make sure to add the plugin to your Vite config
- Use the correct import path
- Be aware of the breaking changes between major versions

**Usage:**
```typescript
import { svelte } from '@sveltejs/vite-plugin-svelte';
const config = {
  plugins: [svelte()]
};
```

---

# @tailwindcss/postcss@4.2.1 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

PostCSS plugin for Tailwind CSS

---

## @tailwindcss/postcss

**Install:**
```bash
npx tailwindcss init -p
```

**Import:**
```typescript
import tailwindcss from 'tailwindcss/postcss7-compat'
```

**Peer Dependencies:**
- `postcss`
- `autoprefixer`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| mode | string | — | 'jit' |
| prefix | string | — | null |
| important | boolean | — | false |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires PostCSS 7 compatibility mode
- Does not support CSS imports out of the box
- Requires manual configuration file changes

**Usage:**
```typescript
import tailwindcss from 'tailwindcss/postcss7-compat';
module.exports = {
  plugins: [tailwindcss],
};
```

---

# svelte-check@4.4.2 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-19
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A Svelte plugin for checking code

---

## svelte-check

**Install:**
```bash
npx svelte-check init
```

**Import:**
```typescript
import { check } from 'svelte-check'
```

**Peer Dependencies:**
- `svelte-preprocess`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| files | string[] | ✓ | — |
| config | object | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires svelte-preprocess to be installed separately
- Does not support Svelte version < 3.0.0
- Must be used with the 'use client' directive in SvelteKit

**Usage:**
```typescript
import { check } from 'svelte-check'; check(['src/**/*.{svelte,html}'])
```
