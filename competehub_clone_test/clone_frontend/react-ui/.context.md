# @chakra-ui/react@3.34.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Accessible React components for building high-quality web apps and design systems

---

## @chakra-ui/react

**Install:**
```bash
npm i @chakra-ui/react
```

**Import:**
```typescript
import { defineTokens } from '@chakra-ui/react'
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual import of CSS files
- May require additional configuration for Next.js and other frameworks

**Usage:**
```typescript
import { defineTokens } from '@chakra-ui/react'; export const tokens = defineTokens({ colors: { primary: { value: '#0FEE0F' } } });
```

---

# @clerk/react@6.1.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Clerk React library for user authentication and management

---

## ClerkProvider

**Install:**
```bash
npm install @clerk/nextjs
```

**Import:**
```typescript
import { ClerkProvider } from '@clerk/nextjs'
```

**Peer Dependencies:**
- `react`
- `next`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| appearance | object | — | — |
| children | ReactNode | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires a Clerk account and instance ID
- Must be wrapped around the entire application

**Usage:**
```tsx
import { ClerkProvider } from '@clerk/nextjs';

function App() {
  return (
    <ClerkProvider>
      <YourApp />
    </ClerkProvider>
  );
}
```

---

## SignInButton

**Install:**
```bash
npm install @clerk/nextjs
```

**Import:**
```typescript
import { SignInButton } from '@clerk/nextjs'
```

**Peer Dependencies:**
- `react`
- `next`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | ReactNode | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires a Clerk instance ID
- Must be used within a ClerkProvider

**Usage:**
```tsx
import { SignInButton } from '@clerk/nextjs';

function App() {
  return (
    <ClerkProvider>
      <SignInButton>
        Sign In
      </SignInButton>
    </ClerkProvider>
  );
}
```

---

## SignUpButton

**Install:**
```bash
npm install @clerk/nextjs
```

**Import:**
```typescript
import { SignUpButton } from '@clerk/nextjs'
```

**Peer Dependencies:**
- `react`
- `next`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | ReactNode | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires a Clerk instance ID
- Must be used within a ClerkProvider

**Usage:**
```tsx
import { SignUpButton } from '@clerk/nextjs';

function App() {
  return (
    <ClerkProvider>
      <SignUpButton>
        Sign Up
      </SignUpButton>
    </ClerkProvider>
  );
}
```

---

## UserButton

**Install:**
```bash
npm install @clerk/nextjs
```

**Import:**
```typescript
import { UserButton } from '@clerk/nextjs'
```

**Peer Dependencies:**
- `react`
- `next`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires a Clerk instance ID
- Must be used within a ClerkProvider

**Usage:**
```tsx
import { UserButton } from '@clerk/nextjs';

function App() {
  return (
    <ClerkProvider>
      <UserButton />
    </ClerkProvider>
  );
}
```

---

# @fontsource-variable/geist@5.2.8 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Fontsource Variable Geist font

---

## @fontsource-variable/geist

**Install:**
```bash
npx fontsource-cli import @fontsource-variable/geist
```

**Import:**
```typescript
import '@fontsource-variable/geist';
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- Must import the font manually in CSS files
- No peer dependencies mentioned, but ensure @fontsource-variable/geist is installed

**Usage:**
```typescript
import '@fontsource-variable/geist';
font-family: 'Geist Variable', sans-serif;
```

---

# @mantine/core@8.3.17 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A fully featured React components library

---

## MantineProvider

**Install:**
```bash
yarn add @mantine/core @mantine/hooks
```

**Import:**
```typescript
import { MantineProvider } from '@mantine/core';
```

**Peer Dependencies:**
- `@mantine/hooks`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| theme | object | — | — |
| withGlobalStyles | boolean | — | True |
| withNormalizeCSS | boolean | — | True |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required to use 'use client' directive for server components
- Must be used with ColorSchemeScript component

**Usage:**
```tsx
import { MantineProvider } from '@mantine/core';
function App() {
  return (
    <MantineProvider>
      <!-- Your app here -->
    </MantineProvider>
  );
}
```

---

## ColorSchemeScript

**Install:**
```bash
yarn add @mantine/core @mantine/hooks
```

**Import:**
```typescript
import { ColorSchemeScript } from '@mantine/core';
```

**Peer Dependencies:**
- `@mantine/hooks`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| defaultColorScheme | string | — | auto |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Must be used with MantineProvider component

**Usage:**
```tsx
import { ColorSchemeScript } from '@mantine/core';
function App() {
  return (
    <html>
      <head>
        <ColorSchemeScript />
      </head>
      <body>
        <!-- Your app here -->
      </body>
    </html>
  );
}
```

---

# @mantine/hooks@8.3.17 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A fully featured React components library

---

## @mantine/hooks

**Install:**
```bash
yarn add @mantine/hooks
```

**Import:**
```typescript
import { useMove, clamp } from '@mantine/hooks';
```

**Peer Dependencies:**
- `react`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| x | number | ✓ | — |
| value | number | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required peer dependencies: React
- Breaking changes between major versions
- CSS imports must be added manually

**Usage:**
```tsx
import { useState } from 'react'; import { useMove, clamp } from '@mantine/hooks'; function Demo() { const [value, setValue] = useState(0.3); const { ref } = useMove(({ x }) => setValue(clamp(x, 0.1, 0.9))); return <div ref={ref}>Slider</div>; }
```

---

# @tanstack/react-query@5.90.21 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Powerful asynchronous state management, server-state utilities and data fetching

---

## useQuery

**Install:**
```bash
npm install @tanstack/react-query
```

**Import:**
```typescript
import { useQuery } from '@tanstack/react-query'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| queryKey | string[] | ✓ | — |
| queryFn | function | ✓ | — |
| enabled | boolean | — | true |
| retry | number | RetryOptions | — | 3 |
| retryDelay | number | — | 1000 |
| staleTime | number | — | 1000 |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Make sure to handle errors properly
- Use the correct query key to avoid cache collisions
- Be mindful of the retry and retryDelay options to avoid excessive requests

**Usage:**
```tsx
import { useQuery } from '@tanstack/react-query'; function Todos() { const { data, isPending, error } = useQuery({ queryKey: ['todos'], queryFn: () => fetch('/api/todos').then(r => r.json()), }); if (isPending) return <span>Loading...</span>; if (error) return <span>Oops!</span>; return <ul>{data.map(t => <li key={t.id}>{t.title}</li>)}</ul>; }
```

---

# class-variance-authority@0.7.1 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A tiny utility for creating variant-based class names

---

## cva

**Install:**
```bash
npm install class-variance-authority@0.7.1
```

**Import:**
```typescript
import { cva } from 'class-variance-authority'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| base | string | ✓ | — |
| variants | object | — | — |
| compoundVariants | array | — | — |
| defaultVariants | object | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required directives (e.g. 'use client' in Next.js App Router) are not handled by cva
- Deprecated props or methods from previous versions may cause issues
- Breaking changes between major versions (e.g. v4 → v5 API changes) may require updates to your code

**Usage:**
```typescript
const button = cva('button', { variants: { intent: { primary: 'bg-blue-500', secondary: 'bg-white' } } });
```

---

# clsx@2.1.1 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A tiny utility for constructing className strings conditionally.

---

## clsx

**Install:**
```bash
npm install --save clsx
```

**Import:**
```typescript
import clsx from 'clsx';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| inputs | ClassValue[] | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Any falsey values are discarded
- Standalone Boolean values are discarded as well
- Required to handle different input types (strings, objects, arrays, etc.)

**Usage:**
```typescript
clsx('foo', true && 'bar', 'baz'); //=> 'foo bar baz'
```

---

## clsx/lite

**Install:**
```bash
npm install --save clsx
```

**Import:**
```typescript
import clsx from 'clsx/lite';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| inputs | string[] | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Only accepts string arguments
- Any non-string input(s) are ignored

**Usage:**
```typescript
clsx('hello', true && 'foo', false && 'bar'); //=> 'hello foo'
```

---

# cmdk@1.1.1 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A command menu React component that can also be used as an accessible combobox.

---

## Command

**Install:**
```bash
pnpm install cmdk
```

**Import:**
```typescript
import { Command } from 'cmdk'
```

**Peer Dependencies:**
- `react`
- `react-dom`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| value | string | — | null |
| onValueChange | function | — | null |
| filter | function | — | null |
| shouldFilter | boolean | — | true |
| loop | boolean | — | false |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React 18
- Uses React 18 hooks like useId and useSyncExternalStore
- Not compatible with React Native
- Not compatible with concurrent mode

**Usage:**
```tsx
import { Command } from 'cmdk';
const CommandMenu = () => {
  const [value, setValue] = React.useState('');
  return (
    <Command value={value} onValueChange={setValue}>
      <Command.Input />
      <Command.List>
        <Command.Item>Apple</Command.Item>
      </Command.List>
    </Command>
  );
};
```

---

## Command.Dialog

**Install:**
```bash
pnpm install cmdk
```

**Import:**
```typescript
import { Command } from 'cmdk'
```

**Peer Dependencies:**
- `react`
- `react-dom`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| open | boolean | — | false |
| onOpenChange | function | — | null |
| container | HTMLElement | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React 18
- Uses React 18 hooks like useId and useSyncExternalStore
- Not compatible with React Native
- Not compatible with concurrent mode

**Usage:**
```tsx
import { Command } from 'cmdk';
const CommandMenu = () => {
  const [open, setOpen] = React.useState(false);
  return (
    <Command.Dialog open={open} onOpenChange={setOpen}>
      <Command.Input />
      <Command.List>
        <Command.Item>Apple</Command.Item>
      </Command.List>
    </Command.Dialog>
  );
};
```

---

## Command.Input

**Install:**
```bash
pnpm install cmdk
```

**Import:**
```typescript
import { Command } from 'cmdk'
```

**Peer Dependencies:**
- `react`
- `react-dom`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| value | string | — | null |
| onValueChange | function | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React 18
- Uses React 18 hooks like useId and useSyncExternalStore
- Not compatible with React Native
- Not compatible with concurrent mode

**Usage:**
```tsx
import { Command } from 'cmdk';
const CommandMenu = () => {
  const [value, setValue] = React.useState('');
  return (
    <Command>
      <Command.Input value={value} onValueChange={setValue} />
      <Command.List>
        <Command.Item>Apple</Command.Item>
      </Command.List>
    </Command>
  );
};
```

---

## Command.List

**Install:**
```bash
pnpm install cmdk
```

**Import:**
```typescript
import { Command } from 'cmdk'
```

**Peer Dependencies:**
- `react`
- `react-dom`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React 18
- Uses React 18 hooks like useId and useSyncExternalStore
- Not compatible with React Native
- Not compatible with concurrent mode

**Usage:**
```tsx
import { Command } from 'cmdk';
const CommandMenu = () => {
  return (
    <Command>
      <Command.Input />
      <Command.List>
        <Command.Item>Apple</Command.Item>
      </Command.List>
    </Command>
  );
};
```

---

## Command.Item

**Install:**
```bash
pnpm install cmdk
```

**Import:**
```typescript
import { Command } from 'cmdk'
```

**Peer Dependencies:**
- `react`
- `react-dom`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| value | string | — | null |
| onSelect | function | — | null |
| keywords | string[] | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React 18
- Uses React 18 hooks like useId and useSyncExternalStore
- Not compatible with React Native
- Not compatible with concurrent mode

**Usage:**
```tsx
import { Command } from 'cmdk';
const CommandMenu = () => {
  return (
    <Command>
      <Command.Input />
      <Command.List>
        <Command.Item value="Apple" onSelect={() => console.log('Selected Apple')}>Apple</Command.Item>
      </Command.List>
    </Command>
  );
};
```

---

## Command.Group

**Install:**
```bash
pnpm install cmdk
```

**Import:**
```typescript
import { Command } from 'cmdk'
```

**Peer Dependencies:**
- `react`
- `react-dom`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| heading | string | ✓ | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React 18
- Uses React 18 hooks like useId and useSyncExternalStore
- Not compatible with React Native
- Not compatible with concurrent mode

**Usage:**
```tsx
import { Command } from 'cmdk';
const CommandMenu = () => {
  return (
    <Command>
      <Command.Input />
      <Command.List>
        <Command.Group heading="Fruit">
          <Command.Item>Apple</Command.Item>
        </Command.Group>
      </Command.List>
    </Command>
  );
};
```

---

## Command.Separator

**Install:**
```bash
pnpm install cmdk
```

**Import:**
```typescript
import { Command } from 'cmdk'
```

**Peer Dependencies:**
- `react`
- `react-dom`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React 18
- Uses React 18 hooks like useId and useSyncExternalStore
- Not compatible with React Native
- Not compatible with concurrent mode

**Usage:**
```tsx
import { Command } from 'cmdk';
const CommandMenu = () => {
  return (
    <Command>
      <Command.Input />
      <Command.List>
        <Command.Item>Apple</Command.Item>
        <Command.Separator />
        <Command.Item>Pear</Command.Item>
      </Command.List>
    </Command>
  );
};
```

---

## Command.Empty

**Install:**
```bash
pnpm install cmdk
```

**Import:**
```typescript
import { Command } from 'cmdk'
```

**Peer Dependencies:**
- `react`
- `react-dom`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React 18
- Uses React 18 hooks like useId and useSyncExternalStore
- Not compatible with React Native
- Not compatible with concurrent mode

**Usage:**
```tsx
import { Command } from 'cmdk';
const CommandMenu = () => {
  return (
    <Command>
      <Command.Input />
      <Command.List>
        <Command.Empty>No results found.</Command.Empty>
      </Command.List>
    </Command>
  );
};
```

---

## Command.Loading

**Install:**
```bash
pnpm install cmdk
```

**Import:**
```typescript
import { Command } from 'cmdk'
```

**Peer Dependencies:**
- `react`
- `react-dom`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires React 18
- Uses React 18 hooks like useId and useSyncExternalStore
- Not compatible with React Native
- Not compatible with concurrent mode

**Usage:**
```tsx
import { Command } from 'cmdk';
const CommandMenu = () => {
  const [loading, setLoading] = React.useState(false);
  return (
    <Command>
      <Command.Input />
      <Command.List>
        {loading && <Command.Loading>Hang on…</Command.Loading>}
      </Command.List>
    </Command>
  );
};
```

---

# embla-carousel-react@8.6.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A lightweight carousel library with fluid motion and great swipe precision

---

## EmblaCarousel

**Install:**
```bash
npm install embla-carousel-react
```

**Import:**
```typescript
import EmblaCarousel from 'embla-carousel-react'
```

**Peer Dependencies:**
- `embla-carousel`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| options | object | — | — |
| ssr | array | — | — |
| breakpoints | object | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required peer dependencies: embla-carousel
- Server-side rendering requires explicit configuration
- CSS imports must be added manually

**Usage:**
```typescript
import EmblaCarousel from 'embla-carousel-react';
const carousel = EmblaCarousel(document.querySelector('.embla'), {});
```

---

# framer-motion@12.37.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A production-ready motion library for React

---

## framer-motion

**Install:**
```bash
npm install framer-motion@12.37.0
```

**Import:**
```typescript
import { motion, useMotionValue, useSpring } from 'framer-motion'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| initial | object | — | null |
| animate | object | — | null |
| transition | object | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required to use 'useClient' directive in Next.js App Router
- Deprecated 'spring' prop in version 12, use 'useSpring' hook instead

**Usage:**
```tsx
import { motion } from 'framer-motion'; function App() { return <motion.div initial={{ x: 0 }} animate={{ x: 100 }} />; }
```

---

# radix-ui@1.4.3 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Unstyled, accessible, open source React primitives for high-quality web apps and design systems.

---

## Dialog

**Install:**
```bash
npm install @radix-ui/react-dialog
```

**Import:**
```typescript
import { Dialog } from '@radix-ui/react-dialog'
```

**Peer Dependencies:**
- `@radix-ui/react-portal`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| open | boolean | — | false |
| onOpenChange | (open: boolean) => void | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual CSS import for styling

**Usage:**
```tsx
import { Dialog } from '@radix-ui/react-dialog';
<Dialog.Root>
  <Dialog.Trigger>View details</Dialog.Trigger>
  <Dialog.Portal>
    <Dialog.Content>
      Content
    </Dialog.Content>
  </Dialog.Portal>
</Dialog.Root>
```

---

## Popover

**Install:**
```bash
npm install @radix-ui/react-popover
```

**Import:**
```typescript
import { Popover } from '@radix-ui/react-popover'
```

**Peer Dependencies:**
- `@radix-ui/react-portal`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| open | boolean | — | false |
| onOpenChange | (open: boolean) => void | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual CSS import for styling

**Usage:**
```tsx
import { Popover } from '@radix-ui/react-popover';
<Popover.Root>
  <Popover.Trigger>View status</Popover.Trigger>
  <Popover.Portal>
    <Popover.Content>
      Content
    </Popover.Content>
  </Popover.Portal>
</Popover.Root>
```

---

## Tooltip

**Install:**
```bash
npm install @radix-ui/react-tooltip
```

**Import:**
```typescript
import { Tooltip } from '@radix-ui/react-tooltip'
```

**Peer Dependencies:**
- `@radix-ui/react-portal`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| open | boolean | — | false |
| onOpenChange | (open: boolean) => void | — | null |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual CSS import for styling

**Usage:**
```tsx
import { Tooltip } from '@radix-ui/react-tooltip';
<Tooltip.Root>
  <Tooltip.Trigger>Hover me</Tooltip.Trigger>
  <Tooltip.Portal>
    <Tooltip.Content>
      Content
    </Tooltip.Content>
  </Tooltip.Portal>
</Tooltip.Root>
```

---

# recharts@3.8.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

React charts

---

## recharts

**Install:**
```bash
npm install recharts react-is
```

**Import:**
```typescript
import { LineChart, XAxis, Tooltip, CartesianGrid, Line } from 'recharts';
```

**Peer Dependencies:**
- `react`
- `react-dom`
- `react-is`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| width | number | ✓ | — |
| height | number | ✓ | — |
| data | array | ✓ | — |
| dataKey | string | ✓ | — |
| stroke | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- react-is needs to match the version of your installed react package
- peerDependencies: react, react-dom, react-is

**Usage:**
```tsx
<LineChart width={400} height={400} data={data}><XAxis dataKey="name" /><Tooltip /><CartesianGrid stroke="#f5f5f5" /><Line type="monotone" dataKey="uv" stroke="#ff7300" /><Line type="monotone" dataKey="pv" stroke="#387908" /></LineChart>
```

---

# shadcn@4.0.8 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A set of beautifully designed components that you can customize, extend, and build on.

---

## shadcn

**Install:**
```bash
pnpm install shadcn@4.0.8
```

**Import:**
```typescript
import { Component } from 'shadcn';
```

**Peer Dependencies:**
- `@types/react`
- `@types/react-dom`
- `react`
- `react-dom`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required peer dependencies must be installed separately
- CSS imports must be added manually

**Usage:**
```tsx
import { Component } from 'shadcn';
function App() {
  return <Component />;
}
```

---

# tw-animate-css@1.4.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

A library for animating CSS with Tailwind CSS

---

## tw-animate-css

**Install:**
```bash
npm install tw-animate-css@1.4.0
```

**Import:**
```typescript
import 'tw-animate-css'
```

**Peer Dependencies:**
- `tailwindcss`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires manual CSS import
- Does not work with older versions of Tailwind CSS

**Usage:**
```typescript
import 'tw-animate-css';
```

---

# vaul@1.1.2 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Drawer component for React

---

## vaul

**Install:**
```bash
pnpm i vaul
```

**Import:**
```typescript
import { Drawer } from 'vaul';
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| defaultOpen | boolean | — | — |
| open | boolean | — | — |
| onOpenChange | function | — | — |
| modal | boolean | — | true |
| container | HTMLElement | — | document.body |
| direction | directionType | — | bottom |
| onAnimationEnd | function | — | — |
| dismissible | boolean | — | true |
| handleOnly | boolean | — | false |
| repositionInputs | boolean | — | true |
| snapPoints | array | — | — |
| activeSnapPoint | boolean | — | — |
| setActiveSnapPoint | function | — | — |
| fadeFromIndex | number | — | — |
| snapToSequentialPoint | boolean | — | — |
| asChild | boolean | — | false |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Required directive 'use client' for Next.js App Router
- Deprecated props or methods from previous versions
- Breaking changes between major versions
- Required peer dependencies that must be installed separately
- Common misuse patterns (wrong import path, wrong prop name)
- Framework-specific requirements (Next.js App Router vs Pages Router)
- CSS imports that must be added manually
- Configuration file changes required

**Usage:**
```tsx
import { Drawer } from 'vaul';

export default function VaulDrawer() {
  return (
    <Drawer.Root>
      <Drawer.Trigger>Open Drawer</Drawer.Trigger>
      <Drawer.Portal>
        <Drawer.Overlay className="fixed inset-0 bg-black/40" />
        <Drawer.Content className="bg-gray-100 h-fit fixed bottom-0 left-0 right-0 outline-none">
          <div className="p-4 bg-white">{/* Content */}</div>
        </Drawer.Content>
      </Drawer.Portal>
    </Drawer.Root>
  );
}
```

---

# @tailwindcss/postcss@4.2.1 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

PostCSS plugin for Tailwind CSS

---

## @tailwindcss/postcss

**Install:**
```bash
npx tailwindcss init -p
```

**Import:**
```typescript
import tailwindcss from 'tailwindcss/postcss7-compat'
```

**Peer Dependencies:**
- `postcss`
- `autoprefixer`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| mode | string | — | 'jit' |
| prefix | string | — | null |
| important | boolean | — | false |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires PostCSS 7 compatibility mode
- Does not support CSS imports out of the box
- Requires manual configuration file changes

**Usage:**
```typescript
import tailwindcss from 'tailwindcss/postcss7-compat';
module.exports = {
  plugins: [tailwindcss],
};
```

---

# @types/node@24.12.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Type definitions for Node.js

---

## @types/node

**Install:**
```bash
npm install --save-dev @types/node@24.12.0
```

**Import:**
```typescript
import * as node from 'node';
```

**Peer Dependencies:**
- `typescript`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires TypeScript 4.6 or higher
- Does not include type definitions for native Node.js modules

**Usage:**
```typescript
import * as fs from 'fs'; fs.readFileSync('file.txt', 'utf8');
```

---

# @vitejs/plugin-react@6.0.0 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Vite Plugin React

---

## @vitejs/plugin-react

**Install:**
```bash
pnpm add @vitejs/plugin-react
```

**Import:**
```typescript
import { react } from '@vitejs/plugin-react'
```

**Peer Dependencies:**
- `vite`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires Node.js version ^20.19.0 or >=22.12.0
- Requires pnpm as package manager
- Deprecated props or methods from previous versions may cause issues

**Usage:**
```typescript
import { defineConfig } from 'vite'; import { react } from '@vitejs/plugin-react'; export default defineConfig({ plugins: [react()] });
```

---

# eslint-plugin-react-refresh@0.5.2 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

ESLint plugin for React Refresh

---

## eslint-plugin-react-refresh

**Install:**
```bash
npx install-peerdeps eslint-plugin-react-refresh
```

**Import:**
```typescript
import 'eslint-plugin-react-refresh'
```

**Peer Dependencies:**
- `eslint`
- `react-refresh`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires eslint version 7 or higher
- Requires react-refresh version 0.11.0 or higher

**Usage:**
```typescript
npx eslint --init && npx install-peerdeps eslint-plugin-react-refresh
```

---

# postcss@8.5.8 — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

PostCSS is a tool for transforming CSS with JavaScript plugins.

---

## postcss

**Install:**
```bash
npm install postcss
```

**Import:**
```typescript
import postcss from 'postcss';
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- Requires peer dependencies to be installed separately
- CSS imports must be added manually

**Usage:**
```typescript
const postcss = require('postcss'); const css = 'body { color: black; }'; postcss([ require('autoprefixer') ]).process(css).then(result => console.log(result.css));
```

---

# reactbits@latest — DocForge Context File
> Generated by DocForge | Last verified: 2026-03-18
> Drop this file into your project and reference it with @context.md in Cursor/Claude

Copy-Paste Component Library (jsrepo/shadcn registry) — 123 components

---

## AnimatedContent

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r AnimatedContent
```

**Import:**
```typescript
import { AnimatedContent } from '@/components/AnimatedContent'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r AnimatedContent` — do NOT use npm install
- Import from your local project: `import { AnimatedContent } from '@/components/AnimatedContent'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { AnimatedContent } from '@/components/AnimatedContent';

export default function Example() {
  return (
    <AnimatedContent />
  );
}
```

---

## BlobCursor

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r BlobCursor
```

**Import:**
```typescript
import { BlobCursor } from '@/components/BlobCursor'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| blobType | 'circle' | 'square' | — | — |
| fillColor | string | — | — |
| trailCount | number | — | — |
| sizes | number[] | — | — |
| innerSizes | number[] | — | — |
| innerColor | string | — | — |
| opacities | number[] | — | — |
| shadowColor | string | — | — |
| shadowBlur | number | — | — |
| shadowOffsetX | number | — | — |
| shadowOffsetY | number | — | — |
| filterId | string | — | — |
| filterStdDeviation | number | — | — |
| filterColorMatrixValues | string | — | — |
| useFilter | boolean | — | — |
| fastDuration | number | — | — |
| slowDuration | number | — | — |
| fastEase | string | — | — |
| slowEase | string | — | — |
| zIndex | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r BlobCursor` — do NOT use npm install
- Import from your local project: `import { BlobCursor } from '@/components/BlobCursor'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { BlobCursor } from '@/components/BlobCursor';

export default function Example() {
  return (
    <BlobCursor
      // blobType={/* value */}
      // fillColor="#3b82f6"
      // trailCount={100}
      // sizes={100}
      // innerSizes={100}
    />
  );
}
```

---

## ClickSpark

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ClickSpark
```

**Import:**
```typescript
import { ClickSpark } from '@/components/ClickSpark'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| sparkColor | string | — | — |
| sparkSize | number | — | — |
| sparkRadius | number | — | — |
| sparkCount | number | — | — |
| duration | number | — | — |
| easing | 'linear' | 'ease-in' | 'ease-out' | 'ease-in-out' | — | — |
| extraScale | number | — | — |
| children | React.ReactNode | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ClickSpark` — do NOT use npm install
- Import from your local project: `import { ClickSpark } from '@/components/ClickSpark'`

**Usage:**
```tsx
import { ClickSpark } from '@/components/ClickSpark';

export default function Example() {
  return (
    <ClickSpark
      // sparkColor="#3b82f6"
      // sparkSize={100}
      // sparkRadius={100}
      // sparkCount={100}
      // duration={1.5}
    >
      {/* content */}
    </ClickSpark>
  );
}
```

---

## Crosshair

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Crosshair
```

**Import:**
```typescript
import { Crosshair } from '@/components/Crosshair'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | string | — | — |
| containerRef | RefObject<HTMLElement> | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Crosshair` — do NOT use npm install
- Import from your local project: `import { Crosshair } from '@/components/Crosshair'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { Crosshair } from '@/components/Crosshair';

export default function Example() {
  return (
    <Crosshair
      // color="#3b82f6"
      // containerRef={<div>content</div>}
    />
  );
}
```

---

## Cubes

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Cubes
```

**Import:**
```typescript
import { Cubes } from '@/components/Cubes'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| gridSize | number | — | — |
| cubeSize | number | — | — |
| maxAngle | number | — | — |
| radius | number | — | — |
| easing | gsap.EaseString | — | — |
| duration | Duration | — | — |
| cellGap | number | Gap | — | — |
| borderStyle | string | — | — |
| faceColor | string | — | — |
| shadow | boolean | string | — | — |
| autoAnimate | boolean | — | — |
| rippleOnClick | boolean | — | — |
| rippleColor | string | — | — |
| rippleSpeed | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Cubes` — do NOT use npm install
- Import from your local project: `import { Cubes } from '@/components/Cubes'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { Cubes } from '@/components/Cubes';

export default function Example() {
  return (
    <Cubes
      // gridSize={100}
      // cubeSize={100}
      // maxAngle={100}
      // radius={100}
      // easing="value"
    />
  );
}
```

---

## ElectricBorder

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ElectricBorder
```

**Import:**
```typescript
import { ElectricBorder } from '@/components/ElectricBorder'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | ReactNode | — | — |
| color | string | — | — |
| speed | number | — | — |
| chaos | number | — | — |
| borderRadius | number | — | — |
| className | string | — | — |
| style | CSSProperties | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ElectricBorder` — do NOT use npm install
- Import from your local project: `import { ElectricBorder } from '@/components/ElectricBorder'`

**Usage:**
```tsx
import { ElectricBorder } from '@/components/ElectricBorder';

export default function Example() {
  return (
    <ElectricBorder
      // color="#3b82f6"
      // speed={300}
      // chaos={0}
      // borderRadius={100}
    >
      {/* content */}
    </ElectricBorder>
  );
}
```

---

## FadeContent

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r FadeContent
```

**Import:**
```typescript
import { FadeContent } from '@/components/FadeContent'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r FadeContent` — do NOT use npm install
- Import from your local project: `import { FadeContent } from '@/components/FadeContent'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { FadeContent } from '@/components/FadeContent';

export default function Example() {
  return (
    <FadeContent />
  );
}
```

---

## GlareHover

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GlareHover
```

**Import:**
```typescript
import { GlareHover } from '@/components/GlareHover'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| width | string | — | — |
| height | string | — | — |
| background | string | — | — |
| borderRadius | string | — | — |
| borderColor | string | — | — |
| children | React.ReactNode | — | — |
| glareColor | string | — | — |
| glareOpacity | number | — | — |
| glareAngle | number | — | — |
| glareSize | number | — | — |
| transitionDuration | number | — | — |
| playOnce | boolean | — | — |
| className | string | — | — |
| style | React.CSSProperties | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GlareHover` — do NOT use npm install
- Import from your local project: `import { GlareHover } from '@/components/GlareHover'`

**Usage:**
```tsx
import { GlareHover } from '@/components/GlareHover';

export default function Example() {
  return (
    <GlareHover
      // width={100}
      // height={100}
      // background="value"
      // borderRadius={100}
      // borderColor="#3b82f6"
    >
      {/* content */}
    </GlareHover>
  );
}
```

---

## GradualBlur

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GradualBlur
```

**Import:**
```typescript
import { GradualBlur } from '@/components/GradualBlur'
```

**Peer Dependencies:**
- `mathjs@^14.6.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| position | 'top' | 'bottom' | 'left' | 'right' | — | — |
| strength | number | — | — |
| height | string | — | — |
| width | string | — | — |
| divCount | number | — | — |
| exponential | boolean | — | — |
| zIndex | number | — | — |
| animated | boolean | 'scroll' | — | — |
| duration | string | — | — |
| easing | string | — | — |
| opacity | number | — | — |
| curve | 'linear' | 'bezier' | 'ease-in' | 'ease-out' | 'ease-in-out' | — | — |
| responsive | boolean | — | — |
| mobileHeight | string | — | — |
| tabletHeight | string | — | — |
| desktopHeight | string | — | — |
| mobileWidth | string | — | — |
| tabletWidth | string | — | — |
| desktopWidth | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GradualBlur` — do NOT use npm install
- Import from your local project: `import { GradualBlur } from '@/components/GradualBlur'`
- Peer dependencies must be installed separately: mathjs@^14.6.0

**Usage:**
```tsx
import { GradualBlur } from '@/components/GradualBlur';

export default function Example() {
  return (
    <GradualBlur
      // position={/* value */}
      // strength={0}
      // height={100}
      // width={100}
      // divCount={100}
    />
  );
}
```

---

## GhostCursor

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GhostCursor
```

**Import:**
```typescript
import { GhostCursor } from '@/components/GhostCursor'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| className | string | — | — |
| style | React.CSSProperties | — | — |
| trailLength | number | — | — |
| inertia | number | — | — |
| grainIntensity | number | — | — |
| bloomStrength | number | — | — |
| bloomRadius | number | — | — |
| bloomThreshold | number | — | — |
| brightness | number | — | — |
| color | string | — | — |
| mixBlendMode | React.CSSProperties['mixBlendMode'] | — | — |
| edgeIntensity | number | — | — |
| maxDevicePixelRatio | number | — | — |
| targetPixels | number | — | — |
| fadeDelayMs | number | — | — |
| fadeDurationMs | number | — | — |
| zIndex | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GhostCursor` — do NOT use npm install
- Import from your local project: `import { GhostCursor } from '@/components/GhostCursor'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { GhostCursor } from '@/components/GhostCursor';

export default function Example() {
  return (
    <GhostCursor
      // trailLength={0}
      // inertia={0}
      // grainIntensity={0}
      // bloomStrength={0}
      // bloomRadius={100}
    />
  );
}
```

---

## ImageTrail

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ImageTrail
```

**Import:**
```typescript
import { ImageTrail } from '@/components/ImageTrail'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | string[] | — | — |
| variant | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ImageTrail` — do NOT use npm install
- Import from your local project: `import { ImageTrail } from '@/components/ImageTrail'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { ImageTrail } from '@/components/ImageTrail';

export default function Example() {
  return (
    <ImageTrail
      // items={[]}
      // variant={0}
    />
  );
}
```

---

## LogoLoop

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r LogoLoop
```

**Import:**
```typescript
import { LogoLoop } from '@/components/LogoLoop'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| logos | LogoItem[] | ✓ | — |
| speed | number | — | — |
| direction | 'left' | 'right' | 'up' | 'down' | — | — |
| width | number | string | — | — |
| logoHeight | number | — | — |
| gap | number | — | — |
| pauseOnHover | boolean | — | — |
| hoverSpeed | number | — | — |
| fadeOut | boolean | — | — |
| fadeOutColor | string | — | — |
| scaleOnHover | boolean | — | — |
| renderItem | (item: LogoItem, key: React.Key) => React.ReactNode | — | — |
| ariaLabel | string | — | — |
| className | string | — | — |
| style | React.CSSProperties | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r LogoLoop` — do NOT use npm install
- Import from your local project: `import { LogoLoop } from '@/components/LogoLoop'`

**Usage:**
```tsx
import { LogoLoop } from '@/components/LogoLoop';

export default function Example() {
  return (
    <LogoLoop
      logos="/path/to/file"
      // speed={300}
      // direction={/* value */}
      // width={100}
      // logoHeight="/path/to/file"
      // gap={0}
    />
  );
}
```

---

## Magnet

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Magnet
```

**Import:**
```typescript
import { Magnet } from '@/components/Magnet'
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Magnet` — do NOT use npm install
- Import from your local project: `import { Magnet } from '@/components/Magnet'`

**Usage:**
```tsx
import { Magnet } from '@/components/Magnet';

export default function Example() {
  return (
    <Magnet />
  );
}
```

---

## MagnetLines

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r MagnetLines
```

**Import:**
```typescript
import { MagnetLines } from '@/components/MagnetLines'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| rows | number | — | — |
| columns | number | — | — |
| containerSize | string | — | — |
| lineColor | string | — | — |
| lineWidth | string | — | — |
| lineHeight | string | — | — |
| baseAngle | number | — | — |
| className | string | — | — |
| style | CSSProperties | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r MagnetLines` — do NOT use npm install
- Import from your local project: `import { MagnetLines } from '@/components/MagnetLines'`

**Usage:**
```tsx
import { MagnetLines } from '@/components/MagnetLines';

export default function Example() {
  return (
    <MagnetLines
      // rows={0}
      // columns={0}
      // containerSize={100}
      // lineColor="#3b82f6"
      // lineWidth={100}
    />
  );
}
```

---

## MetaBalls

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r MetaBalls
```

**Import:**
```typescript
import { MetaBalls } from '@/components/MetaBalls'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | string | — | — |
| speed | number | — | — |
| enableMouseInteraction | boolean | — | — |
| hoverSmoothness | number | — | — |
| animationSize | number | — | — |
| ballCount | number | — | — |
| clumpFactor | number | — | — |
| cursorBallSize | number | — | — |
| cursorBallColor | string | — | — |
| enableTransparency | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r MetaBalls` — do NOT use npm install
- Import from your local project: `import { MetaBalls } from '@/components/MetaBalls'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { MetaBalls } from '@/components/MetaBalls';

export default function Example() {
  return (
    <MetaBalls
      // color="#3b82f6"
      // speed={300}
      // enableMouseInteraction={true}
      // hoverSmoothness={0}
      // animationSize={100}
    />
  );
}
```

---

## MetallicPaint

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r MetallicPaint
```

**Import:**
```typescript
import { MetallicPaint } from '@/components/MetallicPaint'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| imageSrc | string | ✓ | — |
| seed | number | — | — |
| scale | number | — | — |
| refraction | number | — | — |
| blur | number | — | — |
| liquid | number | — | — |
| speed | number | — | — |
| brightness | number | — | — |
| contrast | number | — | — |
| angle | number | — | — |
| fresnel | number | — | — |
| lightColor | string | — | — |
| darkColor | string | — | — |
| patternSharpness | number | — | — |
| waveAmplitude | number | — | — |
| noiseScale | number | — | — |
| chromaticSpread | number | — | — |
| mouseAnimation | boolean | — | — |
| distortion | number | — | — |
| contour | number | — | — |
| tintColor | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r MetallicPaint` — do NOT use npm install
- Import from your local project: `import { MetallicPaint } from '@/components/MetallicPaint'`

**Usage:**
```tsx
import { MetallicPaint } from '@/components/MetallicPaint';

export default function Example() {
  return (
    <MetallicPaint
      imageSrc="/path/to/file"
      // seed={0}
      // scale={0}
      // refraction={0}
      // blur={0}
      // liquid={0}
    />
  );
}
```

---

## Noise

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Noise
```

**Import:**
```typescript
import { Noise } from '@/components/Noise'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| patternSize | number | — | — |
| patternScaleX | number | — | — |
| patternScaleY | number | — | — |
| patternRefreshInterval | number | — | — |
| patternAlpha | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Noise` — do NOT use npm install
- Import from your local project: `import { Noise } from '@/components/Noise'`

**Usage:**
```tsx
import { Noise } from '@/components/Noise';

export default function Example() {
  return (
    <Noise
      // patternSize={100}
      // patternScaleX={0}
      // patternScaleY={0}
      // patternRefreshInterval={0}
      // patternAlpha={0}
    />
  );
}
```

---

## PixelTrail

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r PixelTrail
```

**Import:**
```typescript
import { PixelTrail } from '@/components/PixelTrail'
```

**Peer Dependencies:**
- `@react-three/drei@^10.7.4`
- `@react-three/fiber@^9.3.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| id | string | — | — |
| strength | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r PixelTrail` — do NOT use npm install
- Import from your local project: `import { PixelTrail } from '@/components/PixelTrail'`
- Peer dependencies must be installed separately: @react-three/drei@^10.7.4, @react-three/fiber@^9.3.0, three@^0.167.1

**Usage:**
```tsx
import { PixelTrail } from '@/components/PixelTrail';

export default function Example() {
  return (
    <PixelTrail
      // strength={0}
    />
  );
}
```

---

## PixelTransition

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r PixelTransition
```

**Import:**
```typescript
import { PixelTransition } from '@/components/PixelTransition'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| firstContent | React.ReactNode | string | ✓ | — |
| secondContent | React.ReactNode | string | ✓ | — |
| gridSize | number | — | — |
| pixelColor | string | — | — |
| animationStepDuration | number | — | — |
| once | boolean | — | — |
| className | string | — | — |
| style | CSSProperties | — | — |
| aspectRatio | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r PixelTransition` — do NOT use npm install
- Import from your local project: `import { PixelTransition } from '@/components/PixelTransition'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { PixelTransition } from '@/components/PixelTransition';

export default function Example() {
  return (
    <PixelTransition
      firstContent="value"
      secondContent="value"
      // gridSize={100}
      // pixelColor="#3b82f6"
      // animationStepDuration={1.5}
      // once={true}
      // aspectRatio="value"
    />
  );
}
```

---

## Ribbons

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Ribbons
```

**Import:**
```typescript
import { Ribbons } from '@/components/Ribbons'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| colors | string[] | — | — |
| baseSpring | number | — | — |
| baseFriction | number | — | — |
| baseThickness | number | — | — |
| offsetFactor | number | — | — |
| maxAge | number | — | — |
| pointCount | number | — | — |
| speedMultiplier | number | — | — |
| enableFade | boolean | — | — |
| enableShaderEffect | boolean | — | — |
| effectAmplitude | number | — | — |
| backgroundColor | number[] | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Ribbons` — do NOT use npm install
- Import from your local project: `import { Ribbons } from '@/components/Ribbons'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Ribbons } from '@/components/Ribbons';

export default function Example() {
  return (
    <Ribbons
      // colors="#3b82f6"
      // baseSpring={0}
      // baseFriction={0}
      // baseThickness={0}
      // offsetFactor={0}
    />
  );
}
```

---

## ShapeBlur

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ShapeBlur
```

**Import:**
```typescript
import { ShapeBlur } from '@/components/ShapeBlur'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| className | string | — | — |
| variation | number | — | — |
| pixelRatioProp | number | — | — |
| shapeSize | number | — | — |
| roundness | number | — | — |
| borderSize | number | — | — |
| circleSize | number | — | — |
| circleEdge | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ShapeBlur` — do NOT use npm install
- Import from your local project: `import { ShapeBlur } from '@/components/ShapeBlur'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { ShapeBlur } from '@/components/ShapeBlur';

export default function Example() {
  return (
    <ShapeBlur
      // variation={0}
      // pixelRatioProp={0}
      // shapeSize={100}
      // roundness={0}
      // borderSize={100}
    />
  );
}
```

---

## SplashCursor

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r SplashCursor
```

**Import:**
```typescript
import { SplashCursor } from '@/components/SplashCursor'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| SIM_RESOLUTION | number | — | — |
| DYE_RESOLUTION | number | — | — |
| CAPTURE_RESOLUTION | number | — | — |
| DENSITY_DISSIPATION | number | — | — |
| VELOCITY_DISSIPATION | number | — | — |
| PRESSURE | number | — | — |
| PRESSURE_ITERATIONS | number | — | — |
| CURL | number | — | — |
| SPLAT_RADIUS | number | — | — |
| SPLAT_FORCE | number | — | — |
| SHADING | boolean | — | — |
| COLOR_UPDATE_SPEED | number | — | — |
| BACK_COLOR | ColorRGB | — | — |
| TRANSPARENT | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r SplashCursor` — do NOT use npm install
- Import from your local project: `import { SplashCursor } from '@/components/SplashCursor'`

**Usage:**
```tsx
import { SplashCursor } from '@/components/SplashCursor';

export default function Example() {
  return (
    <SplashCursor
      // SIM_RESOLUTION={0}
      // DYE_RESOLUTION={0}
      // CAPTURE_RESOLUTION={0}
      // DENSITY_DISSIPATION={0}
      // VELOCITY_DISSIPATION={0}
    />
  );
}
```

---

## StarBorder

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r StarBorder
```

**Import:**
```typescript
import { StarBorder } from '@/components/StarBorder'
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r StarBorder` — do NOT use npm install
- Import from your local project: `import { StarBorder } from '@/components/StarBorder'`

**Usage:**
```tsx
import { StarBorder } from '@/components/StarBorder';

export default function Example() {
  return (
    <StarBorder />
  );
}
```

---

## StickerPeel

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r StickerPeel
```

**Import:**
```typescript
import { StickerPeel } from '@/components/StickerPeel'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| imageSrc | string | ✓ | — |
| rotate | number | — | — |
| peelBackHoverPct | number | — | — |
| peelBackActivePct | number | — | — |
| peelEasing | string | — | — |
| peelHoverEasing | string | — | — |
| width | number | — | — |
| shadowIntensity | number | — | — |
| lightingIntensity | number | — | — |
| initialPosition | 'center' | 'random' | { x: number; y: number } | — | — |
| peelDirection | number | — | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r StickerPeel` — do NOT use npm install
- Import from your local project: `import { StickerPeel } from '@/components/StickerPeel'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { StickerPeel } from '@/components/StickerPeel';

export default function Example() {
  return (
    <StickerPeel
      imageSrc="/path/to/file"
      // rotate={0}
      // peelBackHoverPct={0}
      // peelBackActivePct={0}
      // peelEasing="value"
      // peelHoverEasing="value"
    />
  );
}
```

---

## TargetCursor

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r TargetCursor
```

**Import:**
```typescript
import { TargetCursor } from '@/components/TargetCursor'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| targetSelector | string | — | — |
| spinDuration | number | — | — |
| hideDefaultCursor | boolean | — | — |
| hoverDuration | number | — | — |
| parallaxOn | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r TargetCursor` — do NOT use npm install
- Import from your local project: `import { TargetCursor } from '@/components/TargetCursor'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { TargetCursor } from '@/components/TargetCursor';

export default function Example() {
  return (
    <TargetCursor
      // targetSelector="value"
      // spinDuration={1.5}
      // hideDefaultCursor={true}
      // hoverDuration={1.5}
      // parallaxOn={true}
    />
  );
}
```

---

## LaserFlow

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r LaserFlow
```

**Import:**
```typescript
import { LaserFlow } from '@/components/LaserFlow'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| className | string | — | — |
| style | React.CSSProperties | — | — |
| wispDensity | number | — | — |
| dpr | number | — | — |
| mouseSmoothTime | number | — | — |
| mouseTiltStrength | number | — | — |
| horizontalBeamOffset | number | — | — |
| verticalBeamOffset | number | — | — |
| flowSpeed | number | — | — |
| verticalSizing | number | — | — |
| horizontalSizing | number | — | — |
| fogIntensity | number | — | — |
| fogScale | number | — | — |
| wispSpeed | number | — | — |
| wispIntensity | number | — | — |
| flowStrength | number | — | — |
| decay | number | — | — |
| falloffStart | number | — | — |
| fogFallSpeed | number | — | — |
| color | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r LaserFlow` — do NOT use npm install
- Import from your local project: `import { LaserFlow } from '@/components/LaserFlow'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { LaserFlow } from '@/components/LaserFlow';

export default function Example() {
  return (
    <LaserFlow
      // wispDensity={0}
      // dpr={0}
      // mouseSmoothTime={300}
      // mouseTiltStrength={0}
      // horizontalBeamOffset={0}
    />
  );
}
```

---

## Antigravity

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Antigravity
```

**Import:**
```typescript
import { Antigravity } from '@/components/Antigravity'
```

**Peer Dependencies:**
- `@react-three/fiber@^9.3.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| count | number | — | — |
| magnetRadius | number | — | — |
| ringRadius | number | — | — |
| waveSpeed | number | — | — |
| waveAmplitude | number | — | — |
| particleSize | number | — | — |
| lerpSpeed | number | — | — |
| color | string | — | — |
| autoAnimate | boolean | — | — |
| particleVariance | number | — | — |
| rotationSpeed | number | — | — |
| depthFactor | number | — | — |
| pulseSpeed | number | — | — |
| particleShape | 'capsule' | 'sphere' | 'box' | 'tetrahedron' | — | — |
| fieldStrength | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Antigravity` — do NOT use npm install
- Import from your local project: `import { Antigravity } from '@/components/Antigravity'`
- Peer dependencies must be installed separately: @react-three/fiber@^9.3.0, three@^0.167.1

**Usage:**
```tsx
import { Antigravity } from '@/components/Antigravity';

export default function Example() {
  return (
    <Antigravity
      // count={100}
      // magnetRadius={100}
      // ringRadius={100}
      // waveSpeed={300}
      // waveAmplitude={0}
    />
  );
}
```

---

## OrbitImages

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r OrbitImages
```

**Import:**
```typescript
import { OrbitImages } from '@/components/OrbitImages'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| images | string[] | — | — |
| altPrefix | string | — | — |
| shape | OrbitShape | — | — |
| customPath | string | — | — |
| baseWidth | number | — | — |
| radiusX | number | — | — |
| radiusY | number | — | — |
| radius | number | — | — |
| starPoints | number | — | — |
| starInnerRatio | number | — | — |
| rotation | number | — | — |
| duration | number | — | — |
| itemSize | number | — | — |
| direction | 'normal' | 'reverse' | — | — |
| fill | boolean | — | — |
| width | number | '100%' | — | — |
| height | number | 'auto' | — | — |
| className | string | — | — |
| showPath | boolean | — | — |
| pathColor | string | — | — |
| pathWidth | number | — | — |
| easing | 'linear' | 'easeIn' | 'easeOut' | 'easeInOut' | — | — |
| paused | boolean | — | — |
| centerContent | ReactNode | — | — |
| responsive | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r OrbitImages` — do NOT use npm install
- Import from your local project: `import { OrbitImages } from '@/components/OrbitImages'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { OrbitImages } from '@/components/OrbitImages';

export default function Example() {
  return (
    <OrbitImages
      // images="/path/to/file"
      // altPrefix="value"
      // shape={/* value */}
      // customPath="/path/to/file"
      // baseWidth={100}
    />
  );
}
```

---

## MagicRings

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r MagicRings
```

**Import:**
```typescript
import { MagicRings } from '@/components/MagicRings'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | string | — | — |
| colorTwo | string | — | — |
| speed | number | — | — |
| ringCount | number | — | — |
| attenuation | number | — | — |
| lineThickness | number | — | — |
| baseRadius | number | — | — |
| radiusStep | number | — | — |
| scaleRate | number | — | — |
| opacity | number | — | — |
| blur | number | — | — |
| noiseAmount | number | — | — |
| rotation | number | — | — |
| ringGap | number | — | — |
| fadeIn | number | — | — |
| fadeOut | number | — | — |
| followMouse | boolean | — | — |
| mouseInfluence | number | — | — |
| hoverScale | number | — | — |
| parallax | number | — | — |
| clickBurst | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r MagicRings` — do NOT use npm install
- Import from your local project: `import { MagicRings } from '@/components/MagicRings'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { MagicRings } from '@/components/MagicRings';

export default function Example() {
  return (
    <MagicRings
      // color="#3b82f6"
      // colorTwo="#3b82f6"
      // speed={300}
      // ringCount={100}
      // attenuation={0}
    />
  );
}
```

---

## ASCIIText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ASCIIText
```

**Import:**
```typescript
import { ASCIIText } from '@/components/ASCIIText'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| text | string | — | — |
| asciiFontSize | number | — | — |
| textFontSize | number | — | — |
| textColor | string | — | — |
| planeBaseHeight | number | — | — |
| enableWaves | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ASCIIText` — do NOT use npm install
- Import from your local project: `import { ASCIIText } from '@/components/ASCIIText'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { ASCIIText } from '@/components/ASCIIText';

export default function Example() {
  return (
    <ASCIIText
      // text="Hello World"
      // asciiFontSize={100}
      // textFontSize="Hello World"
      // textColor="Hello World"
      // planeBaseHeight={100}
    />
  );
}
```

---

## BlurText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r BlurText
```

**Import:**
```typescript
import { BlurText } from '@/components/BlurText'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| text | string | — | — |
| delay | number | — | — |
| className | string | — | — |
| animateBy | 'words' | 'letters' | — | — |
| direction | 'top' | 'bottom' | — | — |
| threshold | number | — | — |
| rootMargin | string | — | — |
| animationFrom | Record<string, string | number> | — | — |
| animationTo | Array<Record<string, string | number>> | — | — |
| easing | (t: number) => number | — | — |
| onAnimationComplete | () => void | — | — |
| stepDuration | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r BlurText` — do NOT use npm install
- Import from your local project: `import { BlurText } from '@/components/BlurText'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { BlurText } from '@/components/BlurText';

export default function Example() {
  return (
    <BlurText
      // text="Hello World"
      // delay={300}
      // animateBy={/* value */}
      // direction={/* value */}
      // threshold={0}
    />
  );
}
```

---

## CircularText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r CircularText
```

**Import:**
```typescript
import { CircularText } from '@/components/CircularText'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| text | string | ✓ | — |
| spinDuration | number | — | — |
| onHover | 'slowDown' | 'speedUp' | 'pause' | 'goBonkers' | — | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r CircularText` — do NOT use npm install
- Import from your local project: `import { CircularText } from '@/components/CircularText'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { CircularText } from '@/components/CircularText';

export default function Example() {
  return (
    <CircularText
      text="Hello World"
      // spinDuration={1.5}
      // onHover={/* value */}
    />
  );
}
```

---

## CountUp

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r CountUp
```

**Import:**
```typescript
import { CountUp } from '@/components/CountUp'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| to | number | ✓ | — |
| from | number | — | — |
| direction | 'up' | 'down' | — | — |
| delay | number | — | — |
| duration | number | — | — |
| className | string | — | — |
| startWhen | boolean | — | — |
| separator | string | — | — |
| onStart | () => void | — | — |
| onEnd | () => void | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r CountUp` — do NOT use npm install
- Import from your local project: `import { CountUp } from '@/components/CountUp'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { CountUp } from '@/components/CountUp';

export default function Example() {
  return (
    <CountUp
      to={1000}
      // from={0}
      // direction={/* value */}
      // delay={300}
      // duration={1.5}
      // startWhen={true}
    />
  );
}
```

---

## CurvedLoop

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r CurvedLoop
```

**Import:**
```typescript
import { CurvedLoop } from '@/components/CurvedLoop'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| marqueeText | string | — | — |
| speed | number | — | — |
| className | string | — | — |
| curveAmount | number | — | — |
| direction | 'left' | 'right' | — | — |
| interactive | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r CurvedLoop` — do NOT use npm install
- Import from your local project: `import { CurvedLoop } from '@/components/CurvedLoop'`

**Usage:**
```tsx
import { CurvedLoop } from '@/components/CurvedLoop';

export default function Example() {
  return (
    <CurvedLoop
      // marqueeText="Hello World"
      // speed={300}
      // curveAmount={0}
      // direction={/* value */}
      // interactive={true}
    />
  );
}
```

---

## DecryptedText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r DecryptedText
```

**Import:**
```typescript
import { DecryptedText } from '@/components/DecryptedText'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r DecryptedText` — do NOT use npm install
- Import from your local project: `import { DecryptedText } from '@/components/DecryptedText'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { DecryptedText } from '@/components/DecryptedText';

export default function Example() {
  return (
    <DecryptedText />
  );
}
```

---

## FallingText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r FallingText
```

**Import:**
```typescript
import { FallingText } from '@/components/FallingText'
```

**Peer Dependencies:**
- `matter-js@^0.20.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| text | string | — | — |
| highlightWords | string[] | — | — |
| highlightClass | string | — | — |
| trigger | 'auto' | 'scroll' | 'click' | 'hover' | — | — |
| backgroundColor | string | — | — |
| wireframes | boolean | — | — |
| gravity | number | — | — |
| mouseConstraintStiffness | number | — | — |
| fontSize | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r FallingText` — do NOT use npm install
- Import from your local project: `import { FallingText } from '@/components/FallingText'`
- Peer dependencies must be installed separately: matter-js@^0.20.0

**Usage:**
```tsx
import { FallingText } from '@/components/FallingText';

export default function Example() {
  return (
    <FallingText
      // text="Hello World"
      // highlightWords="value"
      // highlightClass="value"
      // trigger={/* value */}
      // backgroundColor="#3b82f6"
    />
  );
}
```

---

## FuzzyText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r FuzzyText
```

**Import:**
```typescript
import { FuzzyText } from '@/components/FuzzyText'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | React.ReactNode | ✓ | — |
| fontSize | number | string | — | — |
| fontWeight | string | number | — | — |
| fontFamily | string | — | — |
| color | string | — | — |
| enableHover | boolean | — | — |
| baseIntensity | number | — | — |
| hoverIntensity | number | — | — |
| fuzzRange | number | — | — |
| fps | number | — | — |
| direction | 'horizontal' | 'vertical' | 'both' | — | — |
| transitionDuration | number | — | — |
| clickEffect | boolean | — | — |
| glitchMode | boolean | — | — |
| glitchInterval | number | — | — |
| glitchDuration | number | — | — |
| gradient | string[] | null | — | — |
| letterSpacing | number | — | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r FuzzyText` — do NOT use npm install
- Import from your local project: `import { FuzzyText } from '@/components/FuzzyText'`

**Usage:**
```tsx
import { FuzzyText } from '@/components/FuzzyText';

export default function Example() {
  return (
    <FuzzyText
      // fontSize={100}
      // fontWeight="value"
      // fontFamily="value"
      // color="#3b82f6"
      // enableHover={true}
    >
      {/* content */}
    </FuzzyText>
  );
}
```

---

## GlitchText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GlitchText
```

**Import:**
```typescript
import { GlitchText } from '@/components/GlitchText'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | string | ✓ | — |
| speed | number | — | — |
| enableShadows | boolean | — | — |
| enableOnHover | boolean | — | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GlitchText` — do NOT use npm install
- Import from your local project: `import { GlitchText } from '@/components/GlitchText'`

**Usage:**
```tsx
import { GlitchText } from '@/components/GlitchText';

export default function Example() {
  return (
    <GlitchText
      // speed={300}
      // enableShadows={true}
      // enableOnHover={true}
    >
      {/* content */}
    </GlitchText>
  );
}
```

---

## GradientText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GradientText
```

**Import:**
```typescript
import { GradientText } from '@/components/GradientText'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | ReactNode | ✓ | — |
| className | string | — | — |
| colors | string[] | — | — |
| animationSpeed | number | — | — |
| showBorder | boolean | — | — |
| direction | 'horizontal' | 'vertical' | 'diagonal' | — | — |
| pauseOnHover | boolean | — | — |
| yoyo | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GradientText` — do NOT use npm install
- Import from your local project: `import { GradientText } from '@/components/GradientText'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { GradientText } from '@/components/GradientText';

export default function Example() {
  return (
    <GradientText
      // colors="#3b82f6"
      // animationSpeed={300}
      // showBorder={true}
      // direction={/* value */}
      // pauseOnHover={true}
    >
      {/* content */}
    </GradientText>
  );
}
```

---

## RotatingText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r RotatingText
```

**Import:**
```typescript
import { RotatingText } from '@/components/RotatingText'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r RotatingText` — do NOT use npm install
- Import from your local project: `import { RotatingText } from '@/components/RotatingText'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { RotatingText } from '@/components/RotatingText';

export default function Example() {
  return (
    <RotatingText />
  );
}
```

---

## ScrambledText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ScrambledText
```

**Import:**
```typescript
import { ScrambledText } from '@/components/ScrambledText'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| radius | number | — | — |
| duration | number | — | — |
| speed | number | — | — |
| scrambleChars | string | — | — |
| className | string | — | — |
| style | React.CSSProperties | — | — |
| children | React.ReactNode | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ScrambledText` — do NOT use npm install
- Import from your local project: `import { ScrambledText } from '@/components/ScrambledText'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { ScrambledText } from '@/components/ScrambledText';

export default function Example() {
  return (
    <ScrambledText
      // radius={100}
      // duration={1.5}
      // speed={300}
      // scrambleChars="value"
    >
      {/* content */}
    </ScrambledText>
  );
}
```

---

## ScrollFloat

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ScrollFloat
```

**Import:**
```typescript
import { ScrollFloat } from '@/components/ScrollFloat'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | ReactNode | ✓ | — |
| scrollContainerRef | RefObject<HTMLElement> | — | — |
| containerClassName | string | — | — |
| textClassName | string | — | — |
| animationDuration | number | — | — |
| ease | string | — | — |
| scrollStart | string | — | — |
| scrollEnd | string | — | — |
| stagger | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ScrollFloat` — do NOT use npm install
- Import from your local project: `import { ScrollFloat } from '@/components/ScrollFloat'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { ScrollFloat } from '@/components/ScrollFloat';

export default function Example() {
  return (
    <ScrollFloat
      // scrollContainerRef={<div>content</div>}
      // containerClassName="value"
      // textClassName="Hello World"
      // animationDuration={1.5}
      // ease="value"
    >
      {/* content */}
    </ScrollFloat>
  );
}
```

---

## ScrollReveal

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ScrollReveal
```

**Import:**
```typescript
import { ScrollReveal } from '@/components/ScrollReveal'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | ReactNode | ✓ | — |
| scrollContainerRef | RefObject<HTMLElement> | — | — |
| enableBlur | boolean | — | — |
| baseOpacity | number | — | — |
| baseRotation | number | — | — |
| blurStrength | number | — | — |
| containerClassName | string | — | — |
| textClassName | string | — | — |
| rotationEnd | string | — | — |
| wordAnimationEnd | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ScrollReveal` — do NOT use npm install
- Import from your local project: `import { ScrollReveal } from '@/components/ScrollReveal'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { ScrollReveal } from '@/components/ScrollReveal';

export default function Example() {
  return (
    <ScrollReveal
      // scrollContainerRef={<div>content</div>}
      // enableBlur={true}
      // baseOpacity={0}
      // baseRotation={0}
      // blurStrength={0}
    >
      {/* content */}
    </ScrollReveal>
  );
}
```

---

## ScrollVelocity

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ScrollVelocity
```

**Import:**
```typescript
import { ScrollVelocity } from '@/components/ScrollVelocity'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | React.ReactNode | ✓ | — |
| baseVelocity | number | ✓ | — |
| scrollContainerRef | React.RefObject<HTMLElement> | — | — |
| className | string | — | — |
| damping | number | — | — |
| stiffness | number | — | — |
| numCopies | number | — | — |
| velocityMapping | VelocityMapping | — | — |
| parallaxClassName | string | — | — |
| scrollerClassName | string | — | — |
| parallaxStyle | React.CSSProperties | — | — |
| scrollerStyle | React.CSSProperties | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ScrollVelocity` — do NOT use npm install
- Import from your local project: `import { ScrollVelocity } from '@/components/ScrollVelocity'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { ScrollVelocity } from '@/components/ScrollVelocity';

export default function Example() {
  return (
    <ScrollVelocity
      baseVelocity={0}
      // scrollContainerRef={<div>content</div>}
      // damping={0}
      // stiffness={0}
      // numCopies={0}
      // velocityMapping={/* value */}
    >
      {/* content */}
    </ScrollVelocity>
  );
}
```

---

## ShinyText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ShinyText
```

**Import:**
```typescript
import { ShinyText } from '@/components/ShinyText'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| text | string | ✓ | — |
| disabled | boolean | — | — |
| speed | number | — | — |
| className | string | — | — |
| color | string | — | — |
| shineColor | string | — | — |
| spread | number | — | — |
| yoyo | boolean | — | — |
| pauseOnHover | boolean | — | — |
| direction | 'left' | 'right' | — | — |
| delay | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ShinyText` — do NOT use npm install
- Import from your local project: `import { ShinyText } from '@/components/ShinyText'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { ShinyText } from '@/components/ShinyText';

export default function Example() {
  return (
    <ShinyText
      text="Hello World"
      // disabled={true}
      // speed={300}
      // color="#3b82f6"
      // shineColor="#3b82f6"
      // spread={0}
    />
  );
}
```

---

## SplitText

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r SplitText
```

**Import:**
```typescript
import { SplitText } from '@/components/SplitText'
```

**Peer Dependencies:**
- `@gsap/react@^2.1.2`
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| text | string | ✓ | — |
| className | string | — | — |
| delay | number | — | — |
| duration | number | — | — |
| ease | string | ((t: number) => number) | — | — |
| splitType | 'chars' | 'words' | 'lines' | 'words, chars' | — | — |
| from | gsap.TweenVars | — | — |
| to | gsap.TweenVars | — | — |
| threshold | number | — | — |
| rootMargin | string | — | — |
| tag | 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'p' | 'span' | — | — |
| textAlign | React.CSSProperties['textAlign'] | — | — |
| onLetterAnimationComplete | () => void | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r SplitText` — do NOT use npm install
- Import from your local project: `import { SplitText } from '@/components/SplitText'`
- Peer dependencies must be installed separately: @gsap/react@^2.1.2, gsap@^3.13.0

**Usage:**
```tsx
import { SplitText } from '@/components/SplitText';

export default function Example() {
  return (
    <SplitText
      text="Hello World"
      // delay={300}
      // duration={1.5}
      // ease="value"
      // splitType={/* value */}
      // from={0}
    />
  );
}
```

---

## TextCursor

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r TextCursor
```

**Import:**
```typescript
import { TextCursor } from '@/components/TextCursor'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| text | string | ✓ | — |
| spacing | number | — | — |
| followMouseDirection | boolean | — | — |
| randomFloat | boolean | — | — |
| exitDuration | number | — | — |
| removalInterval | number | — | — |
| maxPoints | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r TextCursor` — do NOT use npm install
- Import from your local project: `import { TextCursor } from '@/components/TextCursor'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { TextCursor } from '@/components/TextCursor';

export default function Example() {
  return (
    <TextCursor
      text="Hello World"
      // spacing={0}
      // followMouseDirection={true}
      // randomFloat={true}
      // exitDuration={1.5}
      // removalInterval={0}
    />
  );
}
```

---

## TextPressure

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r TextPressure
```

**Import:**
```typescript
import { TextPressure } from '@/components/TextPressure'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| text | string | — | — |
| fontFamily | string | — | — |
| fontUrl | string | — | — |
| width | boolean | — | — |
| weight | boolean | — | — |
| italic | boolean | — | — |
| alpha | boolean | — | — |
| flex | boolean | — | — |
| stroke | boolean | — | — |
| scale | boolean | — | — |
| textColor | string | — | — |
| strokeColor | string | — | — |
| className | string | — | — |
| minFontSize | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r TextPressure` — do NOT use npm install
- Import from your local project: `import { TextPressure } from '@/components/TextPressure'`

**Usage:**
```tsx
import { TextPressure } from '@/components/TextPressure';

export default function Example() {
  return (
    <TextPressure
      // text="Hello World"
      // fontFamily="value"
      // fontUrl="/path/to/file"
      // width={100}
      // weight={true}
    />
  );
}
```

---

## TextType

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r TextType
```

**Import:**
```typescript
import { TextType } from '@/components/TextType'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| className | string | — | — |
| showCursor | boolean | — | — |
| hideCursorWhileTyping | boolean | — | — |
| cursorCharacter | string | React.ReactNode | — | — |
| cursorBlinkDuration | number | — | — |
| cursorClassName | string | — | — |
| text | string | string[] | ✓ | — |
| as | ElementType | — | — |
| typingSpeed | number | — | — |
| initialDelay | number | — | — |
| pauseDuration | number | — | — |
| deletingSpeed | number | — | — |
| loop | boolean | — | — |
| textColors | string[] | — | — |
| variableSpeed | { min: number; max: number } | — | — |
| onSentenceComplete | (sentence: string, index: number) => void | — | — |
| startOnVisible | boolean | — | — |
| reverseMode | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r TextType` — do NOT use npm install
- Import from your local project: `import { TextType } from '@/components/TextType'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { TextType } from '@/components/TextType';

export default function Example() {
  return (
    <TextType
      text="Hello World"
      // showCursor={true}
      // hideCursorWhileTyping={true}
      // cursorCharacter="value"
      // cursorBlinkDuration={1.5}
      // cursorClassName="value"
    />
  );
}
```

---

## TrueFocus

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r TrueFocus
```

**Import:**
```typescript
import { TrueFocus } from '@/components/TrueFocus'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| sentence | string | — | — |
| separator | string | — | — |
| manualMode | boolean | — | — |
| blurAmount | number | — | — |
| borderColor | string | — | — |
| glowColor | string | — | — |
| animationDuration | number | — | — |
| pauseBetweenAnimations | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r TrueFocus` — do NOT use npm install
- Import from your local project: `import { TrueFocus } from '@/components/TrueFocus'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { TrueFocus } from '@/components/TrueFocus';

export default function Example() {
  return (
    <TrueFocus
      // sentence="Hello World"
      // separator="value"
      // manualMode={true}
      // blurAmount={0}
      // borderColor="#3b82f6"
    />
  );
}
```

---

## VariableProximity

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r VariableProximity
```

**Import:**
```typescript
import { VariableProximity } from '@/components/VariableProximity'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r VariableProximity` — do NOT use npm install
- Import from your local project: `import { VariableProximity } from '@/components/VariableProximity'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { VariableProximity } from '@/components/VariableProximity';

export default function Example() {
  return (
    <VariableProximity />
  );
}
```

---

## Shuffle

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Shuffle
```

**Import:**
```typescript
import { Shuffle } from '@/components/Shuffle'
```

**Peer Dependencies:**
- `@gsap/react@^2.1.2`
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| text | string | ✓ | — |
| className | string | — | — |
| style | React.CSSProperties | — | — |
| shuffleDirection | 'left' | 'right' | 'up' | 'down' | — | — |
| duration | number | — | — |
| maxDelay | number | — | — |
| ease | string | ((t: number) => number) | — | — |
| threshold | number | — | — |
| rootMargin | string | — | — |
| tag | 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'p' | 'span' | — | — |
| textAlign | React.CSSProperties['textAlign'] | — | — |
| onShuffleComplete | () => void | — | — |
| shuffleTimes | number | — | — |
| animationMode | 'random' | 'evenodd' | — | — |
| loop | boolean | — | — |
| loopDelay | number | — | — |
| stagger | number | — | — |
| scrambleCharset | string | — | — |
| colorFrom | string | — | — |
| colorTo | string | — | — |
| triggerOnce | boolean | — | — |
| respectReducedMotion | boolean | — | — |
| triggerOnHover | ... | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Shuffle` — do NOT use npm install
- Import from your local project: `import { Shuffle } from '@/components/Shuffle'`
- Peer dependencies must be installed separately: @gsap/react@^2.1.2, gsap@^3.13.0

**Usage:**
```tsx
import { Shuffle } from '@/components/Shuffle';

export default function Example() {
  return (
    <Shuffle
      text="Hello World"
      // shuffleDirection={/* value */}
      // duration={1.5}
      // maxDelay={300}
      // ease="value"
      // threshold={0}
    />
  );
}
```

---

## AnimatedList

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r AnimatedList
```

**Import:**
```typescript
import { AnimatedList } from '@/components/AnimatedList'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | ReactNode | ✓ | — |
| delay | number | — | — |
| index | number | ✓ | — |
| onMouseEnter | MouseEventHandler<HTMLDivElement> | — | — |
| onClick | MouseEventHandler<HTMLDivElement> | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r AnimatedList` — do NOT use npm install
- Import from your local project: `import { AnimatedList } from '@/components/AnimatedList'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { AnimatedList } from '@/components/AnimatedList';

export default function Example() {
  return (
    <AnimatedList
      index={0}
      // delay={300}
      // onMouseEnter={<div>content</div>}
      // onClick={<div>content</div>}
    >
      {/* content */}
    </AnimatedList>
  );
}
```

---

## BounceCards

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r BounceCards
```

**Import:**
```typescript
import { BounceCards } from '@/components/BounceCards'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| className | string | — | — |
| images | string[] | — | — |
| containerWidth | number | — | — |
| containerHeight | number | — | — |
| animationDelay | number | — | — |
| animationStagger | number | — | — |
| easeType | string | — | — |
| transformStyles | string[] | — | — |
| enableHover | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r BounceCards` — do NOT use npm install
- Import from your local project: `import { BounceCards } from '@/components/BounceCards'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { BounceCards } from '@/components/BounceCards';

export default function Example() {
  return (
    <BounceCards
      // images="/path/to/file"
      // containerWidth={100}
      // containerHeight={100}
      // animationDelay={300}
      // animationStagger={0}
    />
  );
}
```

---

## BubbleMenu

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r BubbleMenu
```

**Import:**
```typescript
import { BubbleMenu } from '@/components/BubbleMenu'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| logo | ReactNode | string | ✓ | — |
| onMenuClick | (open: boolean) => void | — | — |
| className | string | — | — |
| style | CSSProperties | — | — |
| menuAriaLabel | string | — | — |
| menuBg | string | — | — |
| menuContentColor | string | — | — |
| useFixedPosition | boolean | — | — |
| items | MenuItem[] | — | — |
| animationEase | string | — | — |
| animationDuration | number | — | — |
| staggerDelay | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r BubbleMenu` — do NOT use npm install
- Import from your local project: `import { BubbleMenu } from '@/components/BubbleMenu'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { BubbleMenu } from '@/components/BubbleMenu';

export default function Example() {
  return (
    <BubbleMenu
      logo="/path/to/file"
      // onMenuClick={true}
      // menuAriaLabel="Hello World"
      // menuBg="value"
      // menuContentColor="#3b82f6"
      // useFixedPosition={true}
    />
  );
}
```

---

## CardNav

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r CardNav
```

**Import:**
```typescript
import { CardNav } from '@/components/CardNav'
```

**Peer Dependencies:**
- `gsap@^3.13.0`
- `react-icons@^5.5.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| logo | string | ✓ | — |
| logoAlt | string | — | — |
| items | CardNavItem[] | ✓ | — |
| className | string | — | — |
| ease | string | — | — |
| baseColor | string | — | — |
| menuColor | string | — | — |
| buttonBgColor | string | — | — |
| buttonTextColor | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r CardNav` — do NOT use npm install
- Import from your local project: `import { CardNav } from '@/components/CardNav'`
- Peer dependencies must be installed separately: gsap@^3.13.0, react-icons@^5.5.0

**Usage:**
```tsx
import { CardNav } from '@/components/CardNav';

export default function Example() {
  return (
    <CardNav
      logo="/path/to/file"
      items={[]}
      // logoAlt="/path/to/file"
      // ease="value"
      // baseColor="#3b82f6"
      // menuColor="#3b82f6"
      // buttonBgColor="#3b82f6"
    />
  );
}
```

---

## CardSwap

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r CardSwap
```

**Import:**
```typescript
import { CardSwap } from '@/components/CardSwap'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| width | number | string | — | — |
| height | number | string | — | — |
| cardDistance | number | — | — |
| verticalDistance | number | — | — |
| delay | number | — | — |
| pauseOnHover | boolean | — | — |
| onCardClick | (idx: number) => void | — | — |
| skewAmount | number | — | — |
| easing | 'linear' | 'elastic' | — | — |
| children | ReactNode | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r CardSwap` — do NOT use npm install
- Import from your local project: `import { CardSwap } from '@/components/CardSwap'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { CardSwap } from '@/components/CardSwap';

export default function Example() {
  return (
    <CardSwap
      // width={100}
      // height={100}
      // cardDistance={0}
      // verticalDistance={0}
      // delay={300}
    >
      {/* content */}
    </CardSwap>
  );
}
```

---

## Carousel

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Carousel
```

**Import:**
```typescript
import { Carousel } from '@/components/Carousel'
```

**Peer Dependencies:**
- `motion@^12.23.12`
- `react-icons@^5.5.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | CarouselItem[] | — | — |
| baseWidth | number | — | — |
| autoplay | boolean | — | — |
| autoplayDelay | number | — | — |
| pauseOnHover | boolean | — | — |
| loop | boolean | — | — |
| round | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Carousel` — do NOT use npm install
- Import from your local project: `import { Carousel } from '@/components/Carousel'`
- Peer dependencies must be installed separately: motion@^12.23.12, react-icons@^5.5.0

**Usage:**
```tsx
import { Carousel } from '@/components/Carousel';

export default function Example() {
  return (
    <Carousel
      // items={[]}
      // baseWidth={100}
      // autoplay={true}
      // autoplayDelay={300}
      // pauseOnHover={true}
    />
  );
}
```

---

## ChromaGrid

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ChromaGrid
```

**Import:**
```typescript
import { ChromaGrid } from '@/components/ChromaGrid'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | ChromaItem[] | — | — |
| className | string | — | — |
| radius | number | — | — |
| columns | number | — | — |
| rows | number | — | — |
| damping | number | — | — |
| fadeOut | number | — | — |
| ease | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ChromaGrid` — do NOT use npm install
- Import from your local project: `import { ChromaGrid } from '@/components/ChromaGrid'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { ChromaGrid } from '@/components/ChromaGrid';

export default function Example() {
  return (
    <ChromaGrid
      // items={[]}
      // radius={100}
      // columns={0}
      // rows={0}
      // damping={0}
    />
  );
}
```

---

## CircularGallery

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r CircularGallery
```

**Import:**
```typescript
import { CircularGallery } from '@/components/CircularGallery'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| gl | GL | ✓ | — |
| plane | Mesh | ✓ | — |
| renderer | Renderer | ✓ | — |
| text | string | ✓ | — |
| textColor | string | — | — |
| font | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r CircularGallery` — do NOT use npm install
- Import from your local project: `import { CircularGallery } from '@/components/CircularGallery'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { CircularGallery } from '@/components/CircularGallery';

export default function Example() {
  return (
    <CircularGallery
      gl={/* value */}
      plane={/* value */}
      renderer={/* value */}
      text="Hello World"
      // textColor="Hello World"
      // font="value"
    />
  );
}
```

---

## Counter

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Counter
```

**Import:**
```typescript
import { Counter } from '@/components/Counter'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| mv | MotionValue<number> | ✓ | — |
| number | number | ✓ | — |
| height | number | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Counter` — do NOT use npm install
- Import from your local project: `import { Counter } from '@/components/Counter'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { Counter } from '@/components/Counter';

export default function Example() {
  return (
    <Counter
      mv={0}
      number={0}
      height={100}
    />
  );
}
```

---

## DecayCard

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r DecayCard
```

**Import:**
```typescript
import { DecayCard } from '@/components/DecayCard'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| width | number | — | — |
| height | number | — | — |
| image | string | — | — |
| children | ReactNode | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r DecayCard` — do NOT use npm install
- Import from your local project: `import { DecayCard } from '@/components/DecayCard'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { DecayCard } from '@/components/DecayCard';

export default function Example() {
  return (
    <DecayCard
      // width={100}
      // height={100}
      // image="/path/to/file"
    >
      {/* content */}
    </DecayCard>
  );
}
```

---

## Dock

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Dock
```

**Import:**
```typescript
import { Dock } from '@/components/Dock'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | DockItemData[] | ✓ | — |
| className | string | — | — |
| distance | number | — | — |
| panelHeight | number | — | — |
| baseItemSize | number | — | — |
| dockHeight | number | — | — |
| magnification | number | — | — |
| spring | SpringOptions | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Dock` — do NOT use npm install
- Import from your local project: `import { Dock } from '@/components/Dock'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { Dock } from '@/components/Dock';

export default function Example() {
  return (
    <Dock
      items={[]}
      // distance={0}
      // panelHeight={100}
      // baseItemSize={100}
      // dockHeight={100}
      // magnification={0}
    />
  );
}
```

---

## DomeGallery

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r DomeGallery
```

**Import:**
```typescript
import { DomeGallery } from '@/components/DomeGallery'
```

**Peer Dependencies:**
- `@use-gesture/react@^10.2.27`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| images | ImageItem[] | — | — |
| fit | number | — | — |
| fitBasis | 'auto' | 'min' | 'max' | 'width' | 'height' | — | — |
| minRadius | number | — | — |
| maxRadius | number | — | — |
| padFactor | number | — | — |
| overlayBlurColor | string | — | — |
| maxVerticalRotationDeg | number | — | — |
| dragSensitivity | number | — | — |
| enlargeTransitionMs | number | — | — |
| segments | number | — | — |
| dragDampening | number | — | — |
| openedImageWidth | string | — | — |
| openedImageHeight | string | — | — |
| imageBorderRadius | string | — | — |
| openedImageBorderRadius | string | — | — |
| grayscale | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r DomeGallery` — do NOT use npm install
- Import from your local project: `import { DomeGallery } from '@/components/DomeGallery'`
- Peer dependencies must be installed separately: @use-gesture/react@^10.2.27

**Usage:**
```tsx
import { DomeGallery } from '@/components/DomeGallery';

export default function Example() {
  return (
    <DomeGallery
      // images="/path/to/file"
      // fit={0}
      // fitBasis={/* value */}
      // minRadius={100}
      // maxRadius={100}
    />
  );
}
```

---

## ElasticSlider

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ElasticSlider
```

**Import:**
```typescript
import { ElasticSlider } from '@/components/ElasticSlider'
```

**Peer Dependencies:**
- `@chakra-ui/react@^3.20.0`
- `motion@^12.23.12`
- `react-icons@^5.5.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| defaultValue | number | — | — |
| startingValue | number | — | — |
| maxValue | number | — | — |
| className | string | — | — |
| isStepped | boolean | — | — |
| stepSize | number | — | — |
| leftIcon | React.ReactNode | — | — |
| rightIcon | React.ReactNode | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ElasticSlider` — do NOT use npm install
- Import from your local project: `import { ElasticSlider } from '@/components/ElasticSlider'`
- Peer dependencies must be installed separately: @chakra-ui/react@^3.20.0, motion@^12.23.12, react-icons@^5.5.0

**Usage:**
```tsx
import { ElasticSlider } from '@/components/ElasticSlider';

export default function Example() {
  return (
    <ElasticSlider
      // defaultValue={0}
      // startingValue={0}
      // maxValue={100}
      // isStepped={true}
      // stepSize={100}
    />
  );
}
```

---

## FlowingMenu

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r FlowingMenu
```

**Import:**
```typescript
import { FlowingMenu } from '@/components/FlowingMenu'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | MenuItemData[] | — | — |
| speed | number | — | — |
| textColor | string | — | — |
| bgColor | string | — | — |
| marqueeBgColor | string | — | — |
| marqueeTextColor | string | — | — |
| borderColor | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r FlowingMenu` — do NOT use npm install
- Import from your local project: `import { FlowingMenu } from '@/components/FlowingMenu'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { FlowingMenu } from '@/components/FlowingMenu';

export default function Example() {
  return (
    <FlowingMenu
      // items={[]}
      // speed={300}
      // textColor="Hello World"
      // bgColor="#3b82f6"
      // marqueeBgColor="#3b82f6"
    />
  );
}
```

---

## FluidGlass

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r FluidGlass
```

**Import:**
```typescript
import { FluidGlass } from '@/components/FluidGlass'
```

**Peer Dependencies:**
- `@react-three/drei@^10.7.4`
- `@react-three/fiber@^9.3.0`
- `maath@^0.10.8`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| mode | Mode | — | — |
| lensProps | ModeProps | — | — |
| barProps | ModeProps | — | — |
| cubeProps | ModeProps | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r FluidGlass` — do NOT use npm install
- Import from your local project: `import { FluidGlass } from '@/components/FluidGlass'`
- Peer dependencies must be installed separately: @react-three/drei@^10.7.4, @react-three/fiber@^9.3.0, maath@^0.10.8, three@^0.167.1

**Usage:**
```tsx
import { FluidGlass } from '@/components/FluidGlass';

export default function Example() {
  return (
    <FluidGlass
      // mode={/* value */}
      // lensProps={/* value */}
      // barProps={/* value */}
      // cubeProps={/* value */}
    />
  );
}
```

---

## FlyingPosters

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r FlyingPosters
```

**Import:**
```typescript
import { FlyingPosters } from '@/components/FlyingPosters'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r FlyingPosters` — do NOT use npm install
- Import from your local project: `import { FlyingPosters } from '@/components/FlyingPosters'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { FlyingPosters } from '@/components/FlyingPosters';

export default function Example() {
  return (
    <FlyingPosters />
  );
}
```

---

## Folder

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Folder
```

**Import:**
```typescript
import { Folder } from '@/components/Folder'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | string | — | — |
| size | number | — | — |
| items | React.ReactNode[] | — | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Folder` — do NOT use npm install
- Import from your local project: `import { Folder } from '@/components/Folder'`

**Usage:**
```tsx
import { Folder } from '@/components/Folder';

export default function Example() {
  return (
    <Folder
      // color="#3b82f6"
      // size={100}
      // items={[]}
    />
  );
}
```

---

## GlassIcons

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GlassIcons
```

**Import:**
```typescript
import { GlassIcons } from '@/components/GlassIcons'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | GlassIconsItem[] | ✓ | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GlassIcons` — do NOT use npm install
- Import from your local project: `import { GlassIcons } from '@/components/GlassIcons'`

**Usage:**
```tsx
import { GlassIcons } from '@/components/GlassIcons';

export default function Example() {
  return (
    <GlassIcons
      items={[]}
    />
  );
}
```

---

## GlassSurface

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GlassSurface
```

**Import:**
```typescript
import { GlassSurface } from '@/components/GlassSurface'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | React.ReactNode | — | — |
| width | number | string | — | — |
| height | number | string | — | — |
| borderRadius | number | — | — |
| borderWidth | number | — | — |
| brightness | number | — | — |
| opacity | number | — | — |
| blur | number | — | — |
| displace | number | — | — |
| backgroundOpacity | number | — | — |
| saturation | number | — | — |
| distortionScale | number | — | — |
| redOffset | number | — | — |
| greenOffset | number | — | — |
| blueOffset | number | — | — |
| xChannel | 'R' | 'G' | 'B' | — | — |
| yChannel | 'R' | 'G' | 'B' | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GlassSurface` — do NOT use npm install
- Import from your local project: `import { GlassSurface } from '@/components/GlassSurface'`

**Usage:**
```tsx
import { GlassSurface } from '@/components/GlassSurface';

export default function Example() {
  return (
    <GlassSurface
      // width={100}
      // height={100}
      // borderRadius={100}
      // borderWidth={100}
      // brightness={0}
    >
      {/* content */}
    </GlassSurface>
  );
}
```

---

## GooeyNav

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GooeyNav
```

**Import:**
```typescript
import { GooeyNav } from '@/components/GooeyNav'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | GooeyNavItem[] | ✓ | — |
| animationTime | number | — | — |
| particleCount | number | — | — |
| particleDistances | [number, number] | — | — |
| particleR | number | — | — |
| timeVariance | number | — | — |
| colors | number[] | — | — |
| initialActiveIndex | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GooeyNav` — do NOT use npm install
- Import from your local project: `import { GooeyNav } from '@/components/GooeyNav'`

**Usage:**
```tsx
import { GooeyNav } from '@/components/GooeyNav';

export default function Example() {
  return (
    <GooeyNav
      items={[]}
      // animationTime={300}
      // particleCount={100}
      // particleDistances={0}
      // particleR={0}
      // timeVariance={300}
    />
  );
}
```

---

## InfiniteMenu

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r InfiniteMenu
```

**Import:**
```typescript
import { InfiniteMenu } from '@/components/InfiniteMenu'
```

**Peer Dependencies:**
- `gl-matrix@^3.4.3`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | MenuItem[] | — | — |
| scale | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r InfiniteMenu` — do NOT use npm install
- Import from your local project: `import { InfiniteMenu } from '@/components/InfiniteMenu'`
- Peer dependencies must be installed separately: gl-matrix@^3.4.3

**Usage:**
```tsx
import { InfiniteMenu } from '@/components/InfiniteMenu';

export default function Example() {
  return (
    <InfiniteMenu
      // items={[]}
      // scale={0}
    />
  );
}
```

---

## Lanyard

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Lanyard
```

**Import:**
```typescript
import { Lanyard } from '@/components/Lanyard'
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Lanyard` — do NOT use npm install
- Import from your local project: `import { Lanyard } from '@/components/Lanyard'`

**Usage:**
```tsx
import { Lanyard } from '@/components/Lanyard';

export default function Example() {
  return (
    <Lanyard />
  );
}
```

---

## MagicBento

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r MagicBento
```

**Import:**
```typescript
import { MagicBento } from '@/components/MagicBento'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | string | — | — |
| title | string | — | — |
| description | string | — | — |
| label | string | — | — |
| textAutoHide | boolean | — | — |
| disableAnimations | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r MagicBento` — do NOT use npm install
- Import from your local project: `import { MagicBento } from '@/components/MagicBento'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { MagicBento } from '@/components/MagicBento';

export default function Example() {
  return (
    <MagicBento
      // color="#3b82f6"
      // title="Hello World"
      // description="value"
      // label="Hello World"
      // textAutoHide="Hello World"
    />
  );
}
```

---

## Masonry

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Masonry
```

**Import:**
```typescript
import { Masonry } from '@/components/Masonry'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | Item[] | ✓ | — |
| ease | string | — | — |
| duration | number | — | — |
| stagger | number | — | — |
| animateFrom | 'bottom' | 'top' | 'left' | 'right' | 'center' | 'random' | — | — |
| scaleOnHover | boolean | — | — |
| hoverScale | number | — | — |
| blurToFocus | boolean | — | — |
| colorShiftOnHover | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Masonry` — do NOT use npm install
- Import from your local project: `import { Masonry } from '@/components/Masonry'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { Masonry } from '@/components/Masonry';

export default function Example() {
  return (
    <Masonry
      items={[]}
      // ease="value"
      // duration={1.5}
      // stagger={0}
      // animateFrom={/* value */}
      // scaleOnHover={true}
    />
  );
}
```

---

## ModelViewer

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ModelViewer
```

**Import:**
```typescript
import { ModelViewer } from '@/components/ModelViewer'
```

**Peer Dependencies:**
- `@react-three/drei@^10.7.4`
- `@react-three/fiber@^9.3.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| url | string | ✓ | — |
| width | number | string | — | — |
| height | number | string | — | — |
| modelXOffset | number | — | — |
| modelYOffset | number | — | — |
| defaultRotationX | number | — | — |
| defaultRotationY | number | — | — |
| defaultZoom | number | — | — |
| minZoomDistance | number | — | — |
| maxZoomDistance | number | — | — |
| enableMouseParallax | boolean | — | — |
| enableManualRotation | boolean | — | — |
| enableHoverRotation | boolean | — | — |
| enableManualZoom | boolean | — | — |
| ambientIntensity | number | — | — |
| keyLightIntensity | number | — | — |
| fillLightIntensity | number | — | — |
| rimLightIntensity | number | — | — |
| environmentPreset | 'city' | 'sunset' | 'night' | 'dawn' | 'studio' | 'apartment' | 'forest' | 'p... | — | — |
| autoFrame | boolean | — | — |
| placeholderSrc | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ModelViewer` — do NOT use npm install
- Import from your local project: `import { ModelViewer } from '@/components/ModelViewer'`
- Peer dependencies must be installed separately: @react-three/drei@^10.7.4, @react-three/fiber@^9.3.0, three@^0.167.1

**Usage:**
```tsx
import { ModelViewer } from '@/components/ModelViewer';

export default function Example() {
  return (
    <ModelViewer
      url="/path/to/file"
      // width={100}
      // height={100}
      // modelXOffset={0}
      // modelYOffset={0}
      // defaultRotationX={0}
    />
  );
}
```

---

## PillNav

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r PillNav
```

**Import:**
```typescript
import { PillNav } from '@/components/PillNav'
```

**Peer Dependencies:**
- `gsap@^3.13.0`
- `react-router-dom@^6.30.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| logo | string | ✓ | — |
| logoAlt | string | — | — |
| items | PillNavItem[] | ✓ | — |
| activeHref | string | — | — |
| className | string | — | — |
| ease | string | — | — |
| baseColor | string | — | — |
| pillColor | string | — | — |
| hoveredPillTextColor | string | — | — |
| pillTextColor | string | — | — |
| onMobileMenuClick | () => void | — | — |
| initialLoadAnimation | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r PillNav` — do NOT use npm install
- Import from your local project: `import { PillNav } from '@/components/PillNav'`
- Peer dependencies must be installed separately: gsap@^3.13.0, react-router-dom@^6.30.1

**Usage:**
```tsx
import { PillNav } from '@/components/PillNav';

export default function Example() {
  return (
    <PillNav
      logo="/path/to/file"
      items={[]}
      // logoAlt="/path/to/file"
      // activeHref="/path/to/file"
      // ease="value"
      // baseColor="#3b82f6"
      // pillColor="#3b82f6"
    />
  );
}
```

---

## PixelCard

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r PixelCard
```

**Import:**
```typescript
import { PixelCard } from '@/components/PixelCard'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| variant | 'default' | 'blue' | 'yellow' | 'pink' | — | — |
| gap | number | — | — |
| speed | number | — | — |
| colors | string | — | — |
| noFocus | boolean | — | — |
| className | string | — | — |
| children | React.ReactNode | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r PixelCard` — do NOT use npm install
- Import from your local project: `import { PixelCard } from '@/components/PixelCard'`

**Usage:**
```tsx
import { PixelCard } from '@/components/PixelCard';

export default function Example() {
  return (
    <PixelCard
      // variant={/* value */}
      // gap={0}
      // speed={300}
      // colors="#3b82f6"
      // noFocus={true}
    >
      {/* content */}
    </PixelCard>
  );
}
```

---

## ProfileCard

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ProfileCard
```

**Import:**
```typescript
import { ProfileCard } from '@/components/ProfileCard'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| avatarUrl | string | ✓ | — |
| iconUrl | string | — | — |
| grainUrl | string | — | — |
| innerGradient | string | — | — |
| behindGlowEnabled | boolean | — | — |
| behindGlowColor | string | — | — |
| behindGlowSize | string | — | — |
| className | string | — | — |
| enableTilt | boolean | — | — |
| enableMobileTilt | boolean | — | — |
| mobileTiltSensitivity | number | — | — |
| miniAvatarUrl | string | — | — |
| name | string | — | — |
| title | string | — | — |
| handle | string | — | — |
| status | string | — | — |
| contactText | string | — | — |
| showUserInfo | boolean | — | — |
| onContactClick | () => void | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ProfileCard` — do NOT use npm install
- Import from your local project: `import { ProfileCard } from '@/components/ProfileCard'`

**Usage:**
```tsx
import { ProfileCard } from '@/components/ProfileCard';

export default function Example() {
  return (
    <ProfileCard
      avatarUrl="/path/to/file"
      // iconUrl="/path/to/file"
      // grainUrl="/path/to/file"
      // innerGradient="value"
      // behindGlowEnabled={true}
      // behindGlowColor="#3b82f6"
    />
  );
}
```

---

## ScrollStack

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ScrollStack
```

**Import:**
```typescript
import { ScrollStack } from '@/components/ScrollStack'
```

**Peer Dependencies:**
- `lenis@^1.3.13`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| itemClassName | string | — | — |
| children | ReactNode | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ScrollStack` — do NOT use npm install
- Import from your local project: `import { ScrollStack } from '@/components/ScrollStack'`
- Peer dependencies must be installed separately: lenis@^1.3.13

**Usage:**
```tsx
import { ScrollStack } from '@/components/ScrollStack';

export default function Example() {
  return (
    <ScrollStack
      // itemClassName="value"
    >
      {/* content */}
    </ScrollStack>
  );
}
```

---

## SpotlightCard

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r SpotlightCard
```

**Import:**
```typescript
import { SpotlightCard } from '@/components/SpotlightCard'
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r SpotlightCard` — do NOT use npm install
- Import from your local project: `import { SpotlightCard } from '@/components/SpotlightCard'`

**Usage:**
```tsx
import { SpotlightCard } from '@/components/SpotlightCard';

export default function Example() {
  return (
    <SpotlightCard />
  );
}
```

---

## Stack

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Stack
```

**Import:**
```typescript
import { Stack } from '@/components/Stack'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| children | React.ReactNode | ✓ | — |
| onSendToBack | () => void | ✓ | — |
| sensitivity | number | ✓ | — |
| disableDrag | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Stack` — do NOT use npm install
- Import from your local project: `import { Stack } from '@/components/Stack'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { Stack } from '@/components/Stack';

export default function Example() {
  return (
    <Stack
      onSendToBack={() => {}}
      sensitivity={0}
      // disableDrag={true}
    >
      {/* content */}
    </Stack>
  );
}
```

---

## Stepper

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Stepper
```

**Import:**
```typescript
import { Stepper } from '@/components/Stepper'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| step | number | ✓ | — |
| currentStep | number | ✓ | — |
| onStepClick | (clicked: number) => void | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Stepper` — do NOT use npm install
- Import from your local project: `import { Stepper } from '@/components/Stepper'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { Stepper } from '@/components/Stepper';

export default function Example() {
  return (
    <Stepper
      step={0}
      currentStep={0}
      onStepClick={0}
    />
  );
}
```

---

## TiltedCard

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r TiltedCard
```

**Import:**
```typescript
import { TiltedCard } from '@/components/TiltedCard'
```

**Peer Dependencies:**
- `motion@^12.23.12`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| imageSrc | React.ComponentProps<'img'>['src'] | ✓ | — |
| altText | string | — | — |
| captionText | string | — | — |
| containerHeight | React.CSSProperties['height'] | — | — |
| containerWidth | React.CSSProperties['width'] | — | — |
| imageHeight | React.CSSProperties['height'] | — | — |
| imageWidth | React.CSSProperties['width'] | — | — |
| scaleOnHover | number | — | — |
| rotateAmplitude | number | — | — |
| showMobileWarning | boolean | — | — |
| showTooltip | boolean | — | — |
| overlayContent | React.ReactNode | — | — |
| displayOverlayContent | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r TiltedCard` — do NOT use npm install
- Import from your local project: `import { TiltedCard } from '@/components/TiltedCard'`
- Peer dependencies must be installed separately: motion@^12.23.12

**Usage:**
```tsx
import { TiltedCard } from '@/components/TiltedCard';

export default function Example() {
  return (
    <TiltedCard
      imageSrc="/path/to/file"
      // altText="Hello World"
      // captionText="Hello World"
      // containerHeight={100}
      // containerWidth={100}
      // imageHeight="/path/to/file"
    />
  );
}
```

---

## StaggeredMenu

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r StaggeredMenu
```

**Import:**
```typescript
import { StaggeredMenu } from '@/components/StaggeredMenu'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| position | 'left' | 'right' | — | — |
| colors | string[] | — | — |
| items | StaggeredMenuItem[] | — | — |
| socialItems | StaggeredMenuSocialItem[] | — | — |
| displaySocials | boolean | — | — |
| displayItemNumbering | boolean | — | — |
| className | string | — | — |
| logoUrl | string | — | — |
| menuButtonColor | string | — | — |
| openMenuButtonColor | string | — | — |
| accentColor | string | — | — |
| changeMenuColorOnOpen | boolean | — | — |
| closeOnClickAway | boolean | — | — |
| onMenuOpen | () => void | — | — |
| onMenuClose | () => void | — | — |
| isFixed | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r StaggeredMenu` — do NOT use npm install
- Import from your local project: `import { StaggeredMenu } from '@/components/StaggeredMenu'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { StaggeredMenu } from '@/components/StaggeredMenu';

export default function Example() {
  return (
    <StaggeredMenu
      // position={/* value */}
      // colors="#3b82f6"
      // items={[]}
      // socialItems={[]}
      // displaySocials={true}
    />
  );
}
```

---

## ReflectiveCard

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ReflectiveCard
```

**Import:**
```typescript
import { ReflectiveCard } from '@/components/ReflectiveCard'
```

**Peer Dependencies:**
- `lucide-react@^0.542.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| blurStrength | number | — | — |
| color | string | — | — |
| metalness | number | — | — |
| roughness | number | — | — |
| overlayColor | string | — | — |
| displacementStrength | number | — | — |
| noiseScale | number | — | — |
| specularConstant | number | — | — |
| grayscale | number | — | — |
| glassDistortion | number | — | — |
| className | string | — | — |
| style | React.CSSProperties | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ReflectiveCard` — do NOT use npm install
- Import from your local project: `import { ReflectiveCard } from '@/components/ReflectiveCard'`
- Peer dependencies must be installed separately: lucide-react@^0.542.0

**Usage:**
```tsx
import { ReflectiveCard } from '@/components/ReflectiveCard';

export default function Example() {
  return (
    <ReflectiveCard
      // blurStrength={0}
      // color="#3b82f6"
      // metalness={0}
      // roughness={0}
      // overlayColor="#3b82f6"
    />
  );
}
```

---

## Aurora

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Aurora
```

**Import:**
```typescript
import { Aurora } from '@/components/Aurora'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| colorStops | string[] | — | — |
| amplitude | number | — | — |
| blend | number | — | — |
| time | number | — | — |
| speed | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Aurora` — do NOT use npm install
- Import from your local project: `import { Aurora } from '@/components/Aurora'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Aurora } from '@/components/Aurora';

export default function Example() {
  return (
    <Aurora
      // colorStops="#3b82f6"
      // amplitude={0}
      // blend={0}
      // time={300}
      // speed={300}
    />
  );
}
```

---

## Balatro

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Balatro
```

**Import:**
```typescript
import { Balatro } from '@/components/Balatro'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| spinRotation | number | — | — |
| spinSpeed | number | — | — |
| offset | [number, number] | — | — |
| color1 | string | — | — |
| color2 | string | — | — |
| color3 | string | — | — |
| contrast | number | — | — |
| lighting | number | — | — |
| spinAmount | number | — | — |
| pixelFilter | number | — | — |
| spinEase | number | — | — |
| isRotate | boolean | — | — |
| mouseInteraction | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Balatro` — do NOT use npm install
- Import from your local project: `import { Balatro } from '@/components/Balatro'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Balatro } from '@/components/Balatro';

export default function Example() {
  return (
    <Balatro
      // spinRotation={0}
      // spinSpeed={300}
      // offset={0}
      // color1="#3b82f6"
      // color2="#3b82f6"
    />
  );
}
```

---

## Ballpit

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Ballpit
```

**Import:**
```typescript
import { Ballpit } from '@/components/Ballpit'
```

**Peer Dependencies:**
- `gsap@^3.13.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| className | string | — | — |
| followCursor | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Ballpit` — do NOT use npm install
- Import from your local project: `import { Ballpit } from '@/components/Ballpit'`
- Peer dependencies must be installed separately: gsap@^3.13.0, three@^0.167.1

**Usage:**
```tsx
import { Ballpit } from '@/components/Ballpit';

export default function Example() {
  return (
    <Ballpit
      // followCursor={true}
    />
  );
}
```

---

## Beams

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Beams
```

**Import:**
```typescript
import { Beams } from '@/components/Beams'
```

**Peer Dependencies:**
- `@react-three/drei@^10.7.4`
- `@react-three/fiber@^9.3.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| beamWidth | number | — | — |
| beamHeight | number | — | — |
| beamNumber | number | — | — |
| lightColor | string | — | — |
| speed | number | — | — |
| noiseIntensity | number | — | — |
| scale | number | — | — |
| rotation | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Beams` — do NOT use npm install
- Import from your local project: `import { Beams } from '@/components/Beams'`
- Peer dependencies must be installed separately: @react-three/drei@^10.7.4, @react-three/fiber@^9.3.0, three@^0.167.1

**Usage:**
```tsx
import { Beams } from '@/components/Beams';

export default function Example() {
  return (
    <Beams
      // beamWidth={100}
      // beamHeight={100}
      // beamNumber={0}
      // lightColor="#3b82f6"
      // speed={300}
    />
  );
}
```

---

## ColorBends

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ColorBends
```

**Import:**
```typescript
import { ColorBends } from '@/components/ColorBends'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| className | string | — | — |
| style | React.CSSProperties | — | — |
| rotation | number | — | — |
| speed | number | — | — |
| colors | string[] | — | — |
| transparent | boolean | — | — |
| autoRotate | number | — | — |
| scale | number | — | — |
| frequency | number | — | — |
| warpStrength | number | — | — |
| mouseInfluence | number | — | — |
| parallax | number | — | — |
| noise | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ColorBends` — do NOT use npm install
- Import from your local project: `import { ColorBends } from '@/components/ColorBends'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { ColorBends } from '@/components/ColorBends';

export default function Example() {
  return (
    <ColorBends
      // rotation={0}
      // speed={300}
      // colors="#3b82f6"
      // transparent={true}
      // autoRotate={0}
    />
  );
}
```

---

## DarkVeil

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r DarkVeil
```

**Import:**
```typescript
import { DarkVeil } from '@/components/DarkVeil'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| hueShift | number | — | — |
| noiseIntensity | number | — | — |
| scanlineIntensity | number | — | — |
| speed | number | — | — |
| scanlineFrequency | number | — | — |
| warpAmount | number | — | — |
| resolutionScale | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r DarkVeil` — do NOT use npm install
- Import from your local project: `import { DarkVeil } from '@/components/DarkVeil'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { DarkVeil } from '@/components/DarkVeil';

export default function Example() {
  return (
    <DarkVeil
      // hueShift={0}
      // noiseIntensity={0}
      // scanlineIntensity={0}
      // speed={300}
      // scanlineFrequency={0}
    />
  );
}
```

---

## Dither

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Dither
```

**Import:**
```typescript
import { Dither } from '@/components/Dither'
```

**Peer Dependencies:**
- `@react-three/fiber@^9.3.0`
- `@react-three/postprocessing@^3.0.4`
- `postprocessing@^6.36.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| waveSpeed | number | ✓ | — |
| waveFrequency | number | ✓ | — |
| waveAmplitude | number | ✓ | — |
| waveColor | [number, number, number] | ✓ | — |
| colorNum | number | ✓ | — |
| pixelSize | number | ✓ | — |
| disableAnimation | boolean | ✓ | — |
| enableMouseInteraction | boolean | ✓ | — |
| mouseRadius | number | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Dither` — do NOT use npm install
- Import from your local project: `import { Dither } from '@/components/Dither'`
- Peer dependencies must be installed separately: @react-three/fiber@^9.3.0, @react-three/postprocessing@^3.0.4, postprocessing@^6.36.0, three@^0.167.1

**Usage:**
```tsx
import { Dither } from '@/components/Dither';

export default function Example() {
  return (
    <Dither
      waveSpeed={300}
      waveFrequency={0}
      waveAmplitude={0}
      waveColor="#3b82f6"
      colorNum="#3b82f6"
      pixelSize={100}
      disableAnimation={true}
      enableMouseInteraction={true}
      mouseRadius={100}
    />
  );
}
```

---

## DotGrid

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r DotGrid
```

**Import:**
```typescript
import { DotGrid } from '@/components/DotGrid'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| dotSize | number | — | — |
| gap | number | — | — |
| baseColor | string | — | — |
| activeColor | string | — | — |
| proximity | number | — | — |
| speedTrigger | number | — | — |
| shockRadius | number | — | — |
| shockStrength | number | — | — |
| maxSpeed | number | — | — |
| resistance | number | — | — |
| returnDuration | number | — | — |
| className | string | — | — |
| style | React.CSSProperties | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r DotGrid` — do NOT use npm install
- Import from your local project: `import { DotGrid } from '@/components/DotGrid'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { DotGrid } from '@/components/DotGrid';

export default function Example() {
  return (
    <DotGrid
      // dotSize={100}
      // gap={0}
      // baseColor="#3b82f6"
      // activeColor="#3b82f6"
      // proximity={0}
    />
  );
}
```

---

## FaultyTerminal

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r FaultyTerminal
```

**Import:**
```typescript
import { FaultyTerminal } from '@/components/FaultyTerminal'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r FaultyTerminal` — do NOT use npm install
- Import from your local project: `import { FaultyTerminal } from '@/components/FaultyTerminal'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { FaultyTerminal } from '@/components/FaultyTerminal';

export default function Example() {
  return (
    <FaultyTerminal />
  );
}
```

---

## Galaxy

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Galaxy
```

**Import:**
```typescript
import { Galaxy } from '@/components/Galaxy'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| focal | [number, number] | — | — |
| rotation | [number, number] | — | — |
| starSpeed | number | — | — |
| density | number | — | — |
| hueShift | number | — | — |
| disableAnimation | boolean | — | — |
| speed | number | — | — |
| mouseInteraction | boolean | — | — |
| glowIntensity | number | — | — |
| saturation | number | — | — |
| mouseRepulsion | boolean | — | — |
| twinkleIntensity | number | — | — |
| rotationSpeed | number | — | — |
| repulsionStrength | number | — | — |
| autoCenterRepulsion | number | — | — |
| transparent | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Galaxy` — do NOT use npm install
- Import from your local project: `import { Galaxy } from '@/components/Galaxy'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Galaxy } from '@/components/Galaxy';

export default function Example() {
  return (
    <Galaxy
      // focal={0}
      // rotation={0}
      // starSpeed={300}
      // density={0}
      // hueShift={0}
    />
  );
}
```

---

## GradientBlinds

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GradientBlinds
```

**Import:**
```typescript
import { GradientBlinds } from '@/components/GradientBlinds'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| className | string | — | — |
| dpr | number | — | — |
| paused | boolean | — | — |
| gradientColors | string[] | — | — |
| angle | number | — | — |
| noise | number | — | — |
| blindCount | number | — | — |
| blindMinWidth | number | — | — |
| mouseDampening | number | — | — |
| mirrorGradient | boolean | — | — |
| spotlightRadius | number | — | — |
| spotlightSoftness | number | — | — |
| spotlightOpacity | number | — | — |
| distortAmount | number | — | — |
| shineDirection | 'left' | 'right' | — | — |
| mixBlendMode | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GradientBlinds` — do NOT use npm install
- Import from your local project: `import { GradientBlinds } from '@/components/GradientBlinds'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { GradientBlinds } from '@/components/GradientBlinds';

export default function Example() {
  return (
    <GradientBlinds
      // dpr={0}
      // paused={true}
      // gradientColors="#3b82f6"
      // angle={0}
      // noise={0}
    />
  );
}
```

---

## Grainient

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Grainient
```

**Import:**
```typescript
import { Grainient } from '@/components/Grainient'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| timeSpeed | number | — | — |
| colorBalance | number | — | — |
| warpStrength | number | — | — |
| warpFrequency | number | — | — |
| warpSpeed | number | — | — |
| warpAmplitude | number | — | — |
| blendAngle | number | — | — |
| blendSoftness | number | — | — |
| rotationAmount | number | — | — |
| noiseScale | number | — | — |
| grainAmount | number | — | — |
| grainScale | number | — | — |
| grainAnimated | boolean | — | — |
| contrast | number | — | — |
| gamma | number | — | — |
| saturation | number | — | — |
| centerX | number | — | — |
| centerY | number | — | — |
| zoom | number | — | — |
| color1 | string | — | — |
| color2 | string | — | — |
| color3 | string | — | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Grainient` — do NOT use npm install
- Import from your local project: `import { Grainient } from '@/components/Grainient'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Grainient } from '@/components/Grainient';

export default function Example() {
  return (
    <Grainient
      // timeSpeed={300}
      // colorBalance="#3b82f6"
      // warpStrength={0}
      // warpFrequency={0}
      // warpSpeed={300}
    />
  );
}
```

---

## GridScan

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GridScan
```

**Import:**
```typescript
import { GridScan } from '@/components/GridScan'
```

**Peer Dependencies:**
- `face-api.js@^0.22.2`
- `postprocessing@^6.36.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| enableWebcam | boolean | — | — |
| showPreview | boolean | — | — |
| modelsPath | string | — | — |
| sensitivity | number | — | — |
| lineThickness | number | — | — |
| linesColor | string | — | — |
| gridScale | number | — | — |
| lineStyle | 'solid' | 'dashed' | 'dotted' | — | — |
| lineJitter | number | — | — |
| enablePost | boolean | — | — |
| bloomIntensity | number | — | — |
| bloomThreshold | number | — | — |
| bloomSmoothing | number | — | — |
| chromaticAberration | number | — | — |
| noiseIntensity | number | — | — |
| scanColor | string | — | — |
| scanOpacity | number | — | — |
| scanDirection | 'forward' | 'backward' | 'pingpong' | — | — |
| scanSoftness | number | — | — |
| scanGlow | number | — | — |
| scanPhaseTaper | number | — | — |
| scanDuration | number | — | — |
| scanDelay | number | — | — |
| enableGyro | boolean | — | — |
| scanOnClick | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GridScan` — do NOT use npm install
- Import from your local project: `import { GridScan } from '@/components/GridScan'`
- Peer dependencies must be installed separately: face-api.js@^0.22.2, postprocessing@^6.36.0, three@^0.167.1

**Usage:**
```tsx
import { GridScan } from '@/components/GridScan';

export default function Example() {
  return (
    <GridScan
      // enableWebcam={true}
      // showPreview={true}
      // modelsPath="/path/to/file"
      // sensitivity={0}
      // lineThickness={0}
    />
  );
}
```

---

## GridDistortion

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GridDistortion
```

**Import:**
```typescript
import { GridDistortion } from '@/components/GridDistortion'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| grid | number | — | — |
| mouse | number | — | — |
| strength | number | — | — |
| relaxation | number | — | — |
| imageSrc | string | ✓ | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GridDistortion` — do NOT use npm install
- Import from your local project: `import { GridDistortion } from '@/components/GridDistortion'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { GridDistortion } from '@/components/GridDistortion';

export default function Example() {
  return (
    <GridDistortion
      imageSrc="/path/to/file"
      // grid={0}
      // mouse={0}
      // strength={0}
      // relaxation={0}
    />
  );
}
```

---

## GridMotion

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r GridMotion
```

**Import:**
```typescript
import { GridMotion } from '@/components/GridMotion'
```

**Peer Dependencies:**
- `gsap@^3.13.0`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| items | (string | ReactNode)[] | — | — |
| gradientColor | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r GridMotion` — do NOT use npm install
- Import from your local project: `import { GridMotion } from '@/components/GridMotion'`
- Peer dependencies must be installed separately: gsap@^3.13.0

**Usage:**
```tsx
import { GridMotion } from '@/components/GridMotion';

export default function Example() {
  return (
    <GridMotion
      // items={[]}
      // gradientColor="#3b82f6"
    />
  );
}
```

---

## Hyperspeed

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Hyperspeed
```

**Import:**
```typescript
import { Hyperspeed } from '@/components/Hyperspeed'
```

**Peer Dependencies:**
- `postprocessing@^6.36.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| effectOptions | Partial<HyperspeedOptions> | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Hyperspeed` — do NOT use npm install
- Import from your local project: `import { Hyperspeed } from '@/components/Hyperspeed'`
- Peer dependencies must be installed separately: postprocessing@^6.36.0, three@^0.167.1

**Usage:**
```tsx
import { Hyperspeed } from '@/components/Hyperspeed';

export default function Example() {
  return (
    <Hyperspeed
      // effectOptions={/* value */}
    />
  );
}
```

---

## Iridescence

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Iridescence
```

**Import:**
```typescript
import { Iridescence } from '@/components/Iridescence'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | [number, number, number] | — | — |
| speed | number | — | — |
| amplitude | number | — | — |
| mouseReact | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Iridescence` — do NOT use npm install
- Import from your local project: `import { Iridescence } from '@/components/Iridescence'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Iridescence } from '@/components/Iridescence';

export default function Example() {
  return (
    <Iridescence
      // color="#3b82f6"
      // speed={300}
      // amplitude={0}
      // mouseReact={true}
    />
  );
}
```

---

## LetterGlitch

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r LetterGlitch
```

**Import:**
```typescript
import { LetterGlitch } from '@/components/LetterGlitch'
```

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r LetterGlitch` — do NOT use npm install
- Import from your local project: `import { LetterGlitch } from '@/components/LetterGlitch'`

**Usage:**
```tsx
import { LetterGlitch } from '@/components/LetterGlitch';

export default function Example() {
  return (
    <LetterGlitch />
  );
}
```

---

## LightRays

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r LightRays
```

**Import:**
```typescript
import { LightRays } from '@/components/LightRays'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| raysOrigin | RaysOrigin | — | — |
| raysColor | string | — | — |
| raysSpeed | number | — | — |
| lightSpread | number | — | — |
| rayLength | number | — | — |
| pulsating | boolean | — | — |
| fadeDistance | number | — | — |
| saturation | number | — | — |
| followMouse | boolean | — | — |
| mouseInfluence | number | — | — |
| noiseAmount | number | — | — |
| distortion | number | — | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r LightRays` — do NOT use npm install
- Import from your local project: `import { LightRays } from '@/components/LightRays'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { LightRays } from '@/components/LightRays';

export default function Example() {
  return (
    <LightRays
      // raysOrigin={/* value */}
      // raysColor="#3b82f6"
      // raysSpeed={300}
      // lightSpread={0}
      // rayLength={0}
    />
  );
}
```

---

## Lightning

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Lightning
```

**Import:**
```typescript
import { Lightning } from '@/components/Lightning'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| hue | number | — | — |
| xOffset | number | — | — |
| speed | number | — | — |
| intensity | number | — | — |
| size | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Lightning` — do NOT use npm install
- Import from your local project: `import { Lightning } from '@/components/Lightning'`

**Usage:**
```tsx
import { Lightning } from '@/components/Lightning';

export default function Example() {
  return (
    <Lightning
      // hue={0}
      // xOffset={0}
      // speed={300}
      // intensity={0}
      // size={100}
    />
  );
}
```

---

## LiquidChrome

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r LiquidChrome
```

**Import:**
```typescript
import { LiquidChrome } from '@/components/LiquidChrome'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r LiquidChrome` — do NOT use npm install
- Import from your local project: `import { LiquidChrome } from '@/components/LiquidChrome'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { LiquidChrome } from '@/components/LiquidChrome';

export default function Example() {
  return (
    <LiquidChrome />
  );
}
```

---

## Orb

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Orb
```

**Import:**
```typescript
import { Orb } from '@/components/Orb'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| hue | number | — | — |
| hoverIntensity | number | — | — |
| rotateOnHover | boolean | — | — |
| forceHoverState | boolean | — | — |
| backgroundColor | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Orb` — do NOT use npm install
- Import from your local project: `import { Orb } from '@/components/Orb'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Orb } from '@/components/Orb';

export default function Example() {
  return (
    <Orb
      // hue={0}
      // hoverIntensity={0}
      // rotateOnHover={true}
      // forceHoverState={true}
      // backgroundColor="#3b82f6"
    />
  );
}
```

---

## Particles

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Particles
```

**Import:**
```typescript
import { Particles } from '@/components/Particles'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| particleCount | number | — | — |
| particleSpread | number | — | — |
| speed | number | — | — |
| particleColors | string[] | — | — |
| moveParticlesOnHover | boolean | — | — |
| particleHoverFactor | number | — | — |
| alphaParticles | boolean | — | — |
| particleBaseSize | number | — | — |
| sizeRandomness | number | — | — |
| cameraDistance | number | — | — |
| disableRotation | boolean | — | — |
| pixelRatio | number | — | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Particles` — do NOT use npm install
- Import from your local project: `import { Particles } from '@/components/Particles'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Particles } from '@/components/Particles';

export default function Example() {
  return (
    <Particles
      // particleCount={100}
      // particleSpread={0}
      // speed={300}
      // particleColors="#3b82f6"
      // moveParticlesOnHover={true}
    />
  );
}
```

---

## PixelBlast

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r PixelBlast
```

**Import:**
```typescript
import { PixelBlast } from '@/components/PixelBlast'
```

**Peer Dependencies:**
- `postprocessing@^6.36.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| variant | PixelBlastVariant | — | — |
| pixelSize | number | — | — |
| color | string | — | — |
| className | string | — | — |
| style | React.CSSProperties | — | — |
| antialias | boolean | — | — |
| patternScale | number | — | — |
| patternDensity | number | — | — |
| liquid | boolean | — | — |
| liquidStrength | number | — | — |
| liquidRadius | number | — | — |
| pixelSizeJitter | number | — | — |
| enableRipples | boolean | — | — |
| rippleIntensityScale | number | — | — |
| rippleThickness | number | — | — |
| rippleSpeed | number | — | — |
| liquidWobbleSpeed | number | — | — |
| autoPauseOffscreen | boolean | — | — |
| speed | number | — | — |
| transparent | boolean | — | — |
| edgeFade | number | — | — |
| noiseAmount | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r PixelBlast` — do NOT use npm install
- Import from your local project: `import { PixelBlast } from '@/components/PixelBlast'`
- Peer dependencies must be installed separately: postprocessing@^6.36.0, three@^0.167.1

**Usage:**
```tsx
import { PixelBlast } from '@/components/PixelBlast';

export default function Example() {
  return (
    <PixelBlast
      // variant={/* value */}
      // pixelSize={100}
      // color="#3b82f6"
      // antialias={true}
      // patternScale={0}
    />
  );
}
```

---

## Plasma

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Plasma
```

**Import:**
```typescript
import { Plasma } from '@/components/Plasma'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | string | — | — |
| speed | number | — | — |
| direction | 'forward' | 'reverse' | 'pingpong' | — | — |
| scale | number | — | — |
| opacity | number | — | — |
| mouseInteractive | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Plasma` — do NOT use npm install
- Import from your local project: `import { Plasma } from '@/components/Plasma'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Plasma } from '@/components/Plasma';

export default function Example() {
  return (
    <Plasma
      // color="#3b82f6"
      // speed={300}
      // direction={/* value */}
      // scale={0}
      // opacity={0}
    />
  );
}
```

---

## Prism

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Prism
```

**Import:**
```typescript
import { Prism } from '@/components/Prism'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| height | number | — | — |
| baseWidth | number | — | — |
| animationType | 'rotate' | 'hover' | '3drotate' | — | — |
| glow | number | — | — |
| offset | { x?: number; y?: number } | — | — |
| noise | number | — | — |
| transparent | boolean | — | — |
| scale | number | — | — |
| hueShift | number | — | — |
| colorFrequency | number | — | — |
| hoverStrength | number | — | — |
| inertia | number | — | — |
| bloom | number | — | — |
| suspendWhenOffscreen | boolean | — | — |
| timeScale | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Prism` — do NOT use npm install
- Import from your local project: `import { Prism } from '@/components/Prism'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Prism } from '@/components/Prism';

export default function Example() {
  return (
    <Prism
      // height={100}
      // baseWidth={100}
      // animationType={/* value */}
      // glow={0}
      // offset={0}
    />
  );
}
```

---

## PrismaticBurst

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r PrismaticBurst
```

**Import:**
```typescript
import { PrismaticBurst } from '@/components/PrismaticBurst'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| intensity | number | — | — |
| speed | number | — | — |
| animationType | AnimationType | — | — |
| colors | string[] | — | — |
| distort | number | — | — |
| paused | boolean | — | — |
| offset | Offset | — | — |
| hoverDampness | number | — | — |
| rayCount | number | — | — |
| mixBlendMode | React.CSSProperties['mixBlendMode'] | 'none' | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r PrismaticBurst` — do NOT use npm install
- Import from your local project: `import { PrismaticBurst } from '@/components/PrismaticBurst'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { PrismaticBurst } from '@/components/PrismaticBurst';

export default function Example() {
  return (
    <PrismaticBurst
      // intensity={0}
      // speed={300}
      // animationType={/* value */}
      // colors="#3b82f6"
      // distort={0}
    />
  );
}
```

---

## RippleGrid

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r RippleGrid
```

**Import:**
```typescript
import { RippleGrid } from '@/components/RippleGrid'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| enableRainbow | boolean | — | — |
| gridColor | string | — | — |
| rippleIntensity | number | — | — |
| gridSize | number | — | — |
| gridThickness | number | — | — |
| fadeDistance | number | — | — |
| vignetteStrength | number | — | — |
| glowIntensity | number | — | — |
| opacity | number | — | — |
| gridRotation | number | — | — |
| mouseInteraction | boolean | — | — |
| mouseInteractionRadius | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r RippleGrid` — do NOT use npm install
- Import from your local project: `import { RippleGrid } from '@/components/RippleGrid'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { RippleGrid } from '@/components/RippleGrid';

export default function Example() {
  return (
    <RippleGrid
      // enableRainbow={true}
      // gridColor="#3b82f6"
      // rippleIntensity={0}
      // gridSize={100}
      // gridThickness={0}
    />
  );
}
```

---

## Silk

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Silk
```

**Import:**
```typescript
import { Silk } from '@/components/Silk'
```

**Peer Dependencies:**
- `@react-three/fiber@^9.3.0`
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| uniforms | SilkUniforms | ✓ | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Silk` — do NOT use npm install
- Import from your local project: `import { Silk } from '@/components/Silk'`
- Peer dependencies must be installed separately: @react-three/fiber@^9.3.0, three@^0.167.1

**Usage:**
```tsx
import { Silk } from '@/components/Silk';

export default function Example() {
  return (
    <Silk
      uniforms={/* value */}
    />
  );
}
```

---

## ShapeGrid

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r ShapeGrid
```

**Import:**
```typescript
import { ShapeGrid } from '@/components/ShapeGrid'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| direction | 'diagonal' | 'up' | 'right' | 'down' | 'left' | — | — |
| speed | number | — | — |
| borderColor | CanvasStrokeStyle | — | — |
| squareSize | number | — | — |
| hoverFillColor | CanvasStrokeStyle | — | — |
| shape | 'square' | 'hexagon' | 'circle' | 'triangle' | — | — |
| hoverTrailAmount | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r ShapeGrid` — do NOT use npm install
- Import from your local project: `import { ShapeGrid } from '@/components/ShapeGrid'`

**Usage:**
```tsx
import { ShapeGrid } from '@/components/ShapeGrid';

export default function Example() {
  return (
    <ShapeGrid
      // direction={/* value */}
      // speed={300}
      // borderColor="#3b82f6"
      // squareSize={100}
      // hoverFillColor="#3b82f6"
    />
  );
}
```

---

## Threads

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Threads
```

**Import:**
```typescript
import { Threads } from '@/components/Threads'
```

**Peer Dependencies:**
- `ogl@^1.0.11`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | [number, number, number] | — | — |
| amplitude | number | — | — |
| distance | number | — | — |
| enableMouseInteraction | boolean | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Threads` — do NOT use npm install
- Import from your local project: `import { Threads } from '@/components/Threads'`
- Peer dependencies must be installed separately: ogl@^1.0.11

**Usage:**
```tsx
import { Threads } from '@/components/Threads';

export default function Example() {
  return (
    <Threads
      // color="#3b82f6"
      // amplitude={0}
      // distance={0}
      // enableMouseInteraction={true}
    />
  );
}
```

---

## Waves

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r Waves
```

**Import:**
```typescript
import { Waves } from '@/components/Waves'
```

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| lineColor | string | — | — |
| backgroundColor | string | — | — |
| waveSpeedX | number | — | — |
| waveSpeedY | number | — | — |
| waveAmpX | number | — | — |
| waveAmpY | number | — | — |
| xGap | number | — | — |
| yGap | number | — | — |
| friction | number | — | — |
| tension | number | — | — |
| maxCursorMove | number | — | — |
| style | CSSProperties | — | — |
| className | string | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r Waves` — do NOT use npm install
- Import from your local project: `import { Waves } from '@/components/Waves'`

**Usage:**
```tsx
import { Waves } from '@/components/Waves';

export default function Example() {
  return (
    <Waves
      // lineColor="#3b82f6"
      // backgroundColor="#3b82f6"
      // waveSpeedX={300}
      // waveSpeedY={300}
      // waveAmpX={0}
    />
  );
}
```

---

## LiquidEther

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r LiquidEther
```

**Import:**
```typescript
import { LiquidEther } from '@/components/LiquidEther'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| mouseForce | number | — | — |
| cursorSize | number | — | — |
| isViscous | boolean | — | — |
| viscous | number | — | — |
| iterationsViscous | number | — | — |
| iterationsPoisson | number | — | — |
| dt | number | — | — |
| BFECC | boolean | — | — |
| resolution | number | — | — |
| isBounce | boolean | — | — |
| colors | string[] | — | — |
| style | React.CSSProperties | — | — |
| className | string | — | — |
| autoDemo | boolean | — | — |
| autoSpeed | number | — | — |
| autoIntensity | number | — | — |
| takeoverDuration | number | — | — |
| autoResumeDelay | number | — | — |
| autoRampDuration | number | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r LiquidEther` — do NOT use npm install
- Import from your local project: `import { LiquidEther } from '@/components/LiquidEther'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { LiquidEther } from '@/components/LiquidEther';

export default function Example() {
  return (
    <LiquidEther
      // mouseForce={0}
      // cursorSize={100}
      // isViscous={true}
      // viscous={0}
      // iterationsViscous={0}
    />
  );
}
```

---

## FloatingLines

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r FloatingLines
```

**Import:**
```typescript
import { FloatingLines } from '@/components/FloatingLines'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| linesGradient | string[] | — | — |
| enabledWaves | Array<'top' | 'middle' | 'bottom'> | — | — |
| lineCount | number | number[] | — | — |
| lineDistance | number | number[] | — | — |
| topWavePosition | WavePosition | — | — |
| middleWavePosition | WavePosition | — | — |
| bottomWavePosition | WavePosition | — | — |
| animationSpeed | number | — | — |
| interactive | boolean | — | — |
| bendRadius | number | — | — |
| bendStrength | number | — | — |
| mouseDamping | number | — | — |
| parallax | boolean | — | — |
| parallaxStrength | number | — | — |
| mixBlendMode | React.CSSProperties['mixBlendMode'] | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r FloatingLines` — do NOT use npm install
- Import from your local project: `import { FloatingLines } from '@/components/FloatingLines'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { FloatingLines } from '@/components/FloatingLines';

export default function Example() {
  return (
    <FloatingLines
      // linesGradient="value"
      // enabledWaves={[]}
      // lineCount={100}
      // lineDistance={0}
      // topWavePosition={/* value */}
    />
  );
}
```

---

## LightPillar

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r LightPillar
```

**Import:**
```typescript
import { LightPillar } from '@/components/LightPillar'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| topColor | string | — | — |
| bottomColor | string | — | — |
| intensity | number | — | — |
| rotationSpeed | number | — | — |
| interactive | boolean | — | — |
| className | string | — | — |
| glowAmount | number | — | — |
| pillarWidth | number | — | — |
| pillarHeight | number | — | — |
| noiseIntensity | number | — | — |
| mixBlendMode | React.CSSProperties['mixBlendMode'] | — | — |
| pillarRotation | number | — | — |
| quality | 'low' | 'medium' | 'high' | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r LightPillar` — do NOT use npm install
- Import from your local project: `import { LightPillar } from '@/components/LightPillar'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { LightPillar } from '@/components/LightPillar';

export default function Example() {
  return (
    <LightPillar
      // topColor="#3b82f6"
      // bottomColor="#3b82f6"
      // intensity={0}
      // rotationSpeed={300}
      // interactive={true}
    />
  );
}
```

---

## PixelSnow

**Install:**
```bash
npx jsrepo add https://reactbits.dev/r PixelSnow
```

**Import:**
```typescript
import { PixelSnow } from '@/components/PixelSnow'
```

**Peer Dependencies:**
- `three@^0.167.1`

**Props:**
| Prop | Type | Required | Default |
|------|------|----------|---------|
| color | string | — | — |
| flakeSize | number | — | — |
| minFlakeSize | number | — | — |
| pixelResolution | number | — | — |
| speed | number | — | — |
| depthFade | number | — | — |
| farPlane | number | — | — |
| brightness | number | — | — |
| gamma | number | — | — |
| density | number | — | — |
| variant | 'square' | 'round' | 'snowflake' | — | — |
| direction | number | — | — |
| className | string | — | — |
| style | React.CSSProperties | — | — |

**⚠️ Gotchas (Common AI Codegen Errors):**
- Install with `npx jsrepo add https://reactbits.dev/r PixelSnow` — do NOT use npm install
- Import from your local project: `import { PixelSnow } from '@/components/PixelSnow'`
- Peer dependencies must be installed separately: three@^0.167.1

**Usage:**
```tsx
import { PixelSnow } from '@/components/PixelSnow';

export default function Example() {
  return (
    <PixelSnow
      // color="#3b82f6"
      // flakeSize={100}
      // minFlakeSize={100}
      // pixelResolution={0}
      // speed={300}
    />
  );
}
```
